# NESエミュレータ開発のはじめかた（5-2：CPUとPPUの秘密の通信窓口 / スプライトとDMA）

前回（5-1）で、画面の背景を作るための巨大な設計図（ネームテーブル）がPPU側の専用の机「VRAM」上にあることを学びました。

今回は、CPUがどうやってその独立したPPUの机（VRAM）に手を出しているのか、そして「背景」ではなく「動くマリオ（スプライト）」をどうやって描画しているのか、スーパーマリオの要である「ダイナミックな動き」の秘密に迫ります。

---

## 3. CPUはどうやってPPUに指示を出すのか？（PPUポート）

CPUとPPUは全く別のチップなので、**直接相手のメモリ（VRAM）に書き込む事は設計上できません。**
そこで使うのが、アーキテクチャ図で説明した **「PPUポート（特定のメモリ番地）」** という Memory Mapped I/O の仕組みです。

CPUにとっての主人公のメモ帳（RAM）は `0x0000` からですが、`0x2000` 〜 `0x2007` のたった8個の番地は、単なるメモ帳ではなく **「PPUへ直接繋がるVIP専用の秘密の窓口」** になっています。

### `$2006` と `$2007` の魔法：窓口を越えた書き込み

PPUのホワイトボード（VRAM）のどこかに文字のスタンプを置きたい場合、CPUは以下の手順で「住所の手紙」と「荷物」を順番に送ります。

1. **`$2006` (PPUADDR 窓口) に、書き込み先となるVIPルームの「住所（VRMAのアドレス）」を2回に分けて送る。**
   
   VRAMのアドレスは16ビット（2バイト）ですが、PPU窓口は「1バイトずつ」しか受け取れません。
   そのため、CPUは「上の住所」「下の住所」と、封筒を2回に分けて `$2006` の窓口にねじ込みます。
   
   ```rust
   // 例: VRAMの $2000 番地（ネームテーブルの一番左上）に書き込みたい！
   cpu.mem_write(0x2006, 0x20); // 先に上位バイト(上の住所)を送信
   cpu.mem_write(0x2006, 0x00); // 次に下位バイト(下の住所)を送信
   ```

2. **`$2007` (PPUDATA 窓口) に、「書きたい荷物（スタンプの番号）」をドン！と送る。**

   住所さえ設定してしまえば、あとは隣の `$2007` 窓口にデータを書き込むだけです。
   ```rust
   // 例: 0x41番のスタンプ（アルファベットのA）を書き込む！
   cpu.mem_write(0x2007, 0x41); 
   ```

これだけで、PPUは「なるほど、受け取っていたVRAMの `$2000` に `0x41` を書き込めばいいんだな」と裏でこっそりVRAMの机の端っこにスタンプを記録（書き込み）してくれます。

### お節介で親切な「自動インクリメント機能」

さらに強力なことに、`$2007` 窓口に書き込んだ直後、PPUは **「自動的に書き込み先の住所を +1（または +32）進めてくれる」** という超親切設計になっています。
このおかげで、CPU側は毎回 `$2006` で住所を送り直す必要はありません。ただひたすら `$2007` のたった1つの窓口に連続して大量のデータを投げ込むだけで、VRAMの左上から右端まで一気にブロックを敷き詰める（画面を描画する）ことができるのです。

---

## 4. 動くキャラクター（スプライト）と OAM

いま説明した「ネームテーブル（背景）」は、あくまで地面やブロックのように固定されたものです。これらは必ず 8×8 ピクセルのマス目にきっちり沿って描かれます。

しかし、マリオヤクリボーなどのキャラクターは、「横32マス」の格子状の制約を無視して、画面上を1ピクセル単位でぬるぬる自由に飛び回りますよね。
これを実現するのが **スプライト機能** であり、そのデータを保存する専用の引き出しが **OAM (Object Attribute Memory: スプライト用メモリ)** です。

OAMは背景用のVRAMとは異なり、PPUチップの中に内蔵された**256バイトの超小型メモリ**です。
ここには「どのキャラクターを(CHRのスタンプ番号)」「画面の X, Y 座標に」「左右反転するか、背景の後ろに隠すか(属性設定)」という情報が4バイトで1セットになって保存されており、テレビ画面上に**最大64個**まで同時にキャラクターを表示させることができます。
（ファミコンのゲームで、敵がたくさん並ぶと「チカチカと点滅する（スプライト欠け）」のは、横一列に並べられるスプライトの限界数が8個という制限に引っかかっているからです）

### OAM DMA (`$4014`)：超高速転送という必殺技

キャラクター（64個分のOAMデータ）は常に動き回るので、ゲームは「毎フレーム（1秒間に60回）」必ずすべてのマリオやパックマンの座標情報をPPUに送り直さなければなりません。

じゃあこれも `$2006` と `$2007` 窓口を使って、「住所をセットして64体を1バイトずつ投げるの？」と思うかもしれませんが、そんなことをしていると毎フレームの計算時間が全く足りなくなり、ゲームの動きが「スローモーション」になってしまいます。

そこで、ファミコンのCPUバス（配線）には **OAM DMA (Direct Memory Access)** というハードウェアレベルでの通信高速化の必殺技（窓口 `$4014`）が用意されています。

```rust
// CPUのメイン・メモリ $0200〜$02FF に、ゲームプログラムが頑張って計算した
// 準備完了の「マリオたちの256バイトのキャラクターリスト最新版」があるとする。

// $4014 番地（DMA窓口）に「0x02 (アドレスの上2桁)」と1回だけ書き込むと...
cpu.mem_write(0x4014, 0x02);
```

なんとこれを実行した瞬間（CPUが `STA $4014` を実行した瞬間）、ハードウェアが主導権を握り、**CPU側の計算を約512サイクルの間一時停止させて、CPUのメモリ `$0200〜$02FF` にある256バイトのデータを一瞬で全てPPUのOAMに丸ごと転送（コピー）** してくれます！

ファミコンのエミュレータ開発では、この「DMAによる時間停止とデータの一括転送」を正確に実装できるかが、マリオを滑らかに動かすための最大の関門となります。

---

## 5. 次のステップへ

ここまでで、以下の関係が見えてきたはずです。

1. **CPU** がプログラム（PRG-ROM）を読み、一生懸命ゲームの計算（移動・判定）をする。
2. CPU が `$2006` と `$2007` (背景) や、必殺の `$4014` DMA (キャラクター) を通じて、隣の **PPU** に「次の画面はこういう配置にして！」と設計図を送る。
3. **PPU** は送られた設計図と、カセットの中の **CHR-ROM(スタンプタイル)** を組み合わせ、PPU自身のサイクルを回しながらテレビ用の映像を職人技で高速生成する。

では、「いつ計算を終えて」「いつPPUに設計図を送ればいい」のでしょうか？
次回は、ファミコンプログラムにおける**「運命のタイミング（NMI）」**、つまりテレビの走査線とエミュレータの60FPSの同期の魔法について、深く切り込んでいきます！
