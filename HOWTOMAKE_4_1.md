# NESエミュレータ開発のはじめかた（4-1：命令の実装とフラグの真実 / TAX・ADC）

これまで、アーキテクチャや時間の同期という「外側の世界」を作ってきました。ここからは再びCPUの内部に戻り、ファミコンのプログラムでよく使われる**「移動（TAX）」**と**「計算（ADC）」**の2つの命令をピックアップします。

Rustでどう実装するのか、そして実行前と実行後でレジスタ（引き出し）がどう変化するのかを、図解付きで詳しく解説します。

---

## 1. 最もシンプルな移動命令： TAX (Transfer A to X)

一番簡単な「引き出し間の移動（コピー）」命令から見てみましょう。
`TAX` は文字通り、**Aレジスタの中身をXレジスタにコピー**する命令です。データを探しに行く必要がない（アドレッシングモードを使わない）ため、CPUの内部だけで一瞬（2サイクル）で完結します。

### Rustでの実装イメージ

```rust
impl Cpu {
    // 0xAA (TAX) のオペコードが呼ばれたときの処理
    fn tax(&mut self) {
        // 1. Aレジスタの中身をXレジスタにそのままコピーする
        self.x = self.a;
        
        // 2. 変化した先の「Xレジスタの値」を見て、
        //    Pレジスタ（メモ帳）の「ゼロフラグ(Z)」と「ネガティブフラグ(N)」を自動更新する
        self.update_zero_and_negative_flags(self.x);
    }
}
```

### レジスタの変化図（A に `42` が入っていた場合）

*   **実行前**： `A` には `42`、 `X` は `0` でした。
*   **実行後**： `A` の値が `X` にコピーされます（Aの数字が消えるわけではありません）。
*   **フラグの変化**: コピーされた値(42)は「ゼロではない」のでゼロフラグ(Z)は `0` のまま。また一番上のビット（後述）も立っていないので、ネガティブフラグ(N)も `0` になります。

```text
[ 実行前 ]                            [ 実行後 (TAX 実行) ]
📦 A = 42                             📦 A = 42  (そのまま残る)
📦 X =  0   ======( コピー )========> 📦 X = 42  (新しく42が入る🎉)

📝 P = [ N:0, Z:0, C:0 ... ]          📝 P = [ N:0, Z:0, C:0 ... ]
```

ただ数字を移すだけ？と思うかもしれませんが、計算の中心であるAレジスタを空けるために一時的にXに退避させたり、ループのカウンタ（現在何回目か）を初期化したりするときに非常に頻繁に使われる重要な命令です。

---

## 2. ファミコンの足し算の主役： ADC (Add with Carry)

次は計算の中心となる `ADC`（エー・ディー・シー） です。「キャリー（繰り上がり）付き足し算」と呼ばれます。
やりたいことは **「現在の Aレジスタの値 ＋ メモリから取ってきた値 ＋ キャリーフラグ(C)の値」** を計算し、その結果をまたAレジスタに上書き保存することです。

### なぜ「キャリーフラグ(C)」を一緒に足すの？

ファミコン世界の数字は全て「8ビット（最大値は255）」です。しかし、マリオのスコア（例えば 123,456点）のような大きな数字を計算したいときがあります。
そのとき、プログラムは大きな数字を「下桁の255」と「上桁の255」のように分割して計算します。

もし「下の桁の足し算」をしたときに **計算結果が255を超えてしまったら** どうなるでしょうか？あぶれた分が上の桁に「繰り上がり」として伝わらなければ、計算が狂ってしまいます。
ファミコンでは、**「255を超えた（桁あふれした）ら、Pレジスタのキャリーフラグ(C)にこっそり『1』を立てる」**というルールがあります。

そして次の「上の桁の足し算(ADC)」のときに、その「1」も一緒に足し込むことで、何桁になっても正確な筆算ができる（これをバケツリレーと呼びます）という天才的な設計になっているのです。

### Rustでの実装イメージ

```rust
impl Cpu {
    // 0x69 など (ADC) が呼ばれたときの処理
    fn adc(&mut self, mode: &AddressingMode) {
        // 1. 指定されたモードの場所から、足したい相手の数字(data)を持ってくる
        let addr = self.get_operand_address(mode);
        let data = self.mem_read(addr);

        // 2. 現在のPレジスタを見て、キャリーフラグ(C)が立っているかチェック
        //    （立っていれば1、なければ0 にする）
        let carry = if self.p & 0b0000_0001 == 1 { 1 } else { 0 };

        // 3. A自身 ＋ 相手 ＋ キャリー を全て足す！
        //    ※あえて16ビット(u16)の広い箱で計算することで、255を超えるか安全に判定できる
        let sum = self.a as u16 + data as u16 + carry as u16;

        // もし合計が 255 (16進数で0xFF) を超えていたら、桁あふれ発生！
        if sum > 0xFF {
            self.p = self.p | 0b0000_0001; // 次の計算のためにキャリーフラグを ON (1) にする
        } else {
            self.p = self.p & 0b1111_1110; // 越えなければ OFF (0) にする
        }

        // 4. 下8ビット(0〜255の範囲)だけを取り出して、Aレジスタに上書き保存する
        self.a = sum as u8;
        
        // (オーバーフローフラグ(V)という少し複雑な処理もありますが、今回は割愛します)
        
        // 5. お決まりの「ゼロだったか」「マイナスになったか」フラグの更新
        self.update_zero_and_negative_flags(self.a);
    }
}
```

### レジスタの変化図（A=250 に 10 を足す場合）

`250 ＋ 10 ＝ 260`。
しかし「8ビット（最大255）」のAレジスタには入り切りません。そのため、あぶれた分（260 - 256 = 4）だけがAレジスタに残り、「繰り上がったよ！」という証拠がPレジスタのキャリーフラグにバトンとして残されます。

```text
[ 実行前 ]                            [ 実行後 ( ADC #10 実行) ]
📦 A    = 250                         📦 A    = 4    (256が引かれて4だけ残る💦)
📝 data = 10     ==( 250+10+0 )==>    📝 data = 10 

📝 P = [ N:0, Z:0, C:0 ]              📝 P = [ N:0, Z:0, C:1 ] (キャリー発生！💥)
```

この強力な `ADC` があるおかげで、ファミコンはたった255までしか数えられない頭脳でありながら、ドラクエのゴールドやHPなど、何万という数字を扱うことができるのです。

次章（4-2）では、計算したフラグ（メモ帳）を使って、プログラムの流れを枝分かれさせる「分岐命令（BEQ）」の仕組みに迫ります。
