# NESエミュレータ開発の壁（D-2：スタックを使った「関数」の実現（JSR/RTS））

前回（D-1）で、ファミコンのCPUには、`$0100`〜`$01FF` の専用メモリ空間と「SP（スタックポインタ）」を使って、一時的にデータを積み上げる「スタック（お皿）」のハードウェア機能があることを学びました。

今回は、このスタックの仕組みを極限まで利用して、ゲームのプログラムをブロック化する**「サブルーチン（関数＝Function のこと）」へのワープと帰還の指示**、`JSR` と `RTS` について解説します。

---

## 4. プログラムは「関数」の繰り返し

C言語やRustなど、現代の言語には `fn damage_calc()` のような「関数（サブルーチン）」という概念が当たり前に存在します。
関数の中で計算をして、`return` をすれば、関数を呼び出した元の行に自動で戻ってきてくれます。

```rust
// 現代の言語のイメージ
fn main() {
    let score = 100;
    damage_calc(); // ------ ワープ！(JSR) ----+
    // <===== 自動でここに戻ってくる (RTS) <-----+
    let new_score = score - 10;
}

fn damage_calc() {
    // 適当な長い計算（敵の防御力や乱数など）
    return; // -------------------------------+
}
```

アセンブリ言語においても、全く同じことを実現する仕組みがあります。
それが、**`JSR` (Jump to SubRoutine：サブルーチンへ飛べ)** と、**`RTS` (ReTurn from Subroutine：サブルーチンから戻れ)** という、必ずペアで扱われる強力な命令です。

### 「帰り道」を忘れないためのスタック

ただの `JMP`（ジャンプ）命令は「あっちへ飛べ」という片道切符であり、飛んだ先には「自分が元々どこから来たのか（帰り道のアドレス）」を知る術がありません。
では `RTS` 命令は、どうやって「呼び出された元の行（`main`関数の続き）」に戻ってくればいいのでしょうか？

ここで、前回学んだ **「スタック（お皿）」** が大活躍します。

1. **ワープするとき（JSRの発動時）:**
   CPUはジャンプする直前に、自分自身がいま持っている **「現在のプログラムカウンタ（PCの番地）」そのものを、スタック（一番上のお皿）にプッシュして保存** します（これをリターンアドレスと呼びます）。
   PCは16ビットの大きな住所なので、お皿を「2枚（2バイト）」使って保存します。

2. **帰るとき（RTSの発動時）:**
   関数の末尾に置かれた `RTS` を読み取ると、CPUは「あ、終わったから戻れという指示だな！」と即座に理解し、**スタックからお皿を「2枚」ポップして（引き抜いて）先ほど保存した帰り道（PC）を復元** します。
   これによって、無事に `JSR` を命令された直後の行に戻ってくるのです。

---

## 5. Rustでのスタック操作関数と `JSR` の実装

エミュレータを作る際、このスタック（プッシュとポップ）の処理は非常に頻繁に行われるため、CPU構造体の中に専用のヘルパー関数として独立して作成するのが一般的です。

```rust
impl Cpu {
    // スタックに1バイト（お皿を1枚）積む関数
    fn stack_push_u8(&mut self, data: u8) {
        // スタック領域は常に $0100〜$01FF なので、$0100 に SP を足した番地に書き込む
        self.mem_write(0x0100 + self.sp as u16, data);
        
        // 次のためにSPを下へ下げる(1つ減らす) (※ラッピングで0に戻る)
        self.sp = self.sp.wrapping_sub(1);
    }

    // スタックから1バイト（お皿を1枚）取り戻す関数
    fn stack_pop_u8(&mut self) -> u8 {
        // 先取りしてSPを上に上げる(1つ増やす)
        self.sp = self.sp.wrapping_add(1);
        
        // その場所のデータを読んで返す
        self.mem_read(0x0100 + self.sp as u16)
    }

    // 16ビット(PCの番地など)を積んだり降ろしたりするヘルパー関数
    fn stack_push_u16(&mut self, data: u16) {
        let hi = (data >> 8) as u8;   // 上位バイト
        let lo = (data & 0xff) as u8; // 下位バイト
        // 6502は上位バイトから先にプッシュする（リトルエンディアン仕様）
        self.stack_push_u8(hi);
        self.stack_push_u8(lo);
    }

    fn stack_pop_u16(&mut self) -> u16 {
        let lo = self.stack_pop_u8() as u16;
        let hi = self.stack_pop_u8() as u16;
        (hi << 8) | lo
    }
}
```

この強力なヘルパー関数セットを作ってしまえば、最も複雑に見えるジャンプ系・リターン命令も、非常にシンプルに実装することができます。

```rust
impl Cpu {
    // 例: 0x20 (JSR: Jump to SubRoutine) の実装
    fn jsr(&mut self) {
        // 1. 直後にあるジャンプ先(2バイト)を先読みして取得する
        let jump_addr = self.mem_read_u16(self.pc);
        
        // (※6502の特殊な仕様で、PCはあえて「次の命令の1つ手前(+1)」を保存するルールになっています)
        self.pc += 1; // 読むために進めた分
        self.stack_push_u16(self.pc); // 今のPC(帰り道)をスタックに追加！

        // 2. 新しい住所へPCをワープ(ジャンプ)させる
        self.pc = jump_addr;
    }

    // 例: 0x60 (RTS: ReTurn from Subroutine) の実装
    fn rts(&mut self) {
        // 1. スタックから、かつて保存した帰り道の住所を取り出す
        let return_addr = self.stack_pop_u16();

        // 2. PCを取り出した住所に戻し、(※仕様通り) +1 して再開する
        self.pc = return_addr + 1;
    }
}
```

## 6. すべての計算は「スタック」の上に成る

エミュレータでゲームを動かしたとき、この `JSR` と `RTS` は、マリオの座標計算関数、背景描画関数、音楽再生関数など、文字通り**1秒間に数万回という猛烈な速度で呼び出され、スタック（`$0100`〜`$01FF`）の上にお皿が積まれては消えていきます。**

もしスタックの実装で（例えば上位バイトと下位バイトの順番を間違えるなどして）たった一文字でも狂いが生じると、関数から「全く違うメモリの住所」へ帰還してしまい、エミュレータは直ちにクラッシュするか無限ループに陥ります。

しかし、一度この正確なプッシュとポップの回路（Rust関数）を作り上げることができれば、マリオのプログラムは関数の階層という「構造化された文明の利器」を手に入れ、どんなに複雑な大冒険のコードでもバグなく動き続けることができるようになるのです。
