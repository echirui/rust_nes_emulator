# NESエミュレータ開発の壁（E-1：PPUの恐るべき「8サイクル」の仕事量）

ここまでのガイド（その5やA章）で、PPUは「1段ずつ走査線を降りていき、画面を描画し終えると休憩（VBlank）に入る」と解説しました。
そして、その描画のループは「CPUが1歩進むたびに、PPUは3歩進む」という比率で同期していることも学びました（その3、その6）。

しかし、**「PPUはその『3回の歩み（クロック）』の中で、具体的に何を計算しているのか？」** という、ブラックボックスの中身については触れてきませんでした。

今回は、完璧な描画（サイクルアキュレート：Cycle-Accurate）を目指すエミュレータ開発者にとって避けては通れない壁、**PPUの描画パイプライン（8サイクルのフェッチ処理）** の全貌について解説します。

---

## 1. 走査線上の「341歩」の旅

PPUは、縦の走査線（スキャンライン）を0段目から順番に降りていきますが、実は**横の1行（1スキャンラインあたり）を描く間にも「0〜341」という細かな歩み（ドット・サイクル）** を刻んでいます。

つまり、テレビに映る画面の横幅は「256ドット」ですが、PPUから見れば、1行を終えるまでに残りの「85歩分」という見えない作業時間（ブランク期間）が存在するのです。

```text
[ 1本のスキャンライン (0〜341サイクル) の時間の流れ ]

 0サイクル目（左端）============================ 255サイクル目（右端） ----->
 [ ここで「テレビに映る256個のピクセル」を必死に色計算して出力している ]

-----> 256サイクル目 ======== 320サイクル目 ========> 341サイクル目（次行へ）
 [ 次の行(走査線)に表示する「スプライト(敵やマリオ)」などを見つける見えない準備時間 ]
```

---

## 2. 恐怖の「8サイクル・ループ」

画面に見える部分（0〜255サイクル目）をひたすら描いている間、PPUはどのように「背景（1タイル=8x8ピクセル）」の色を決定しているのでしょうか？

PPUは、自らのクロックが「1ピクセル横に進む」たびに色を出力しますが、同時に**「将来使うためのタイル（8ドット分）のデータを VRAM から持ってくる（フェッチする）」** という仕事を、ちょうど**「8サイクル周期」**で並行して行っています。

この8歩のステップは、ファミコンのハードウェア上で完全に固定された厳密な順番で、VRAMのチップからデータを読み取っています。

### 8歩の完全なスケジュール（背景フェッチ）

```text
[ サイクル 1〜2歩目 ]: ナメクジの「N」 (Nametable Byte)
VRAMのネームテーブル($2000等)にアクセスし、「これか描くタイルの番号(スタンプ番号)」を読み取る。

[ サイクル 3〜4歩目 ]: アトリビュートの「A」 (Attribute Table Byte)
VRAMの属性テーブルにアクセスし、「このタイルは何色のパレット(色付け)か」を読み取る。

[ サイクル 5〜6歩目 ]: パターン下位の「L」 (Pattern Table Low Byte)
CHR-ROMのパターンテーブルにアクセスし、スタンプの「下半分の色ビットデータ(8ドット分)」を読み取る。

[ サイクル 7〜8歩目 ]: パターン上位の「H」 (Pattern Table High Byte)
CHR-ROMにアクセスし、スタンプの「上半分の色ビットデータ(8ドット分)」を読み取る。
```

この「N, A, L, H」という4つの読み取り（それぞれ2歩ずつ消費）を、横にスクロールしながら永遠に繰り返しています。

### なぜ「サイクル・アキュレート（完全同期）」が難しいのか？

初期のエミュレータ（簡易モデル）を作るときは、この複雑なパイプラインを無視して、「1行（1スキャンライン）ぶんを関数で一気にバーッと描いて（For文ループ）、一瞬で終わらせる（キャッチアップ方式）」というズルをすることがよくあります。

しかし、実際のゲーム（例えばマリオ3の画面分割や、パンチアウト等の特殊な表示）の中には、**「PPUがちょうど128歩目（画面のど真ん中）の『L』の文字を読み取ろうとしているまさにその瞬間の1ミリ秒に、CPUから強制的にスクロール座標（$2005）を上書きしてごまかす」** といった、狂気のようなハック（Raster Effect）を使っているものが多数存在します。

そのため、高度なエミュレータを作る上では、関数で一気に描くのではなく、この「8歩ループ」をRustの `ppu.tick()` 関数の中でバカ正直に、1歩（1ピクセル刻み）ずつ、何万回も `match` 文などでシミュレーションし続ける必要があるのです。

次章（E-2）では、背景の裏で動いているもう一つの巨大な処理、「スプライト評価（マリオなどの動くキャラクターを探す処理）」と、合成（シフトレジスタ）の終着点について解説します。
