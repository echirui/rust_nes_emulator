# NESエミュレータ開発の壁（E-2：スプライト評価とピクセル合成のシフト回路）

前回（E-1）で、PPUは1回の走査線（0〜341サイクル）の中で、前半（0〜255）は画面を見せつつ「自分用に8歩ごとの決まったリズムで背景データを拾う（フェッチする）」作業を繰り返していることを学びました。

しかし、画面には背景（ブロックや空）だけでなく、マリオやクリボーのような「スプライト（動けるキャラクター）」も同時に描かれています。

今回は、この動くキャラクターたちをPPUがどうやって毎フレーム見つけ出し、最終的に背景と合成してテレビへ出力しているのか（シフトレジスタの仕組み）について解説します。

---

## 3. 次の行への準備「スプライト評価 (Sprite Evaluation)」

PPUのOAM（Object Attribute Memory）には、最大で「64個」のキャラクター情報（X座標、Y座標、デザイン等）が保存されていました（その5参照）。
しかし、ファミコンのPPUは機械的な制約上、**「横方向の1行（1スキャンライン）の中に、最大でおよそ8個までのキャラクターしか並べて描画することができない」** という有名な限界（スプライト欠け・チラつきの原因）を持っています。

この制限を守りながら描画するため、PPUは「今描いている行（例：100段目）」をテレビに映している最中の裏で、急いで**「次の行（101段目）に表示すべきキャラクターは、64個の中からどれとどれか？」** を探してピックアップする（評価する）作業を行っています。

*   **サイクル 1〜64（初期化）:** セカンダリOAMという「次行のための候補リスト（8個分）」を初期状態（0xFF）でクリアします。
*   **サイクル 65〜256（評価）:** OAMの64個を順番に調べ、「キャラクターのY座標に自分の高さ（8ドットか16ドット）を足して、それが『次の行（101段目）』に含まれているか？」を判定し、含まれていれば次々と候補リストにコピー（最大8個まで）していきます。

*   **サイクル 257〜320（スプライト・フェッチ）:**
    先ほどの「背景の8歩ループ（N,A,L,H）」が256歩目で終わると、PPUの仕事の残り時間（テレビにビームが映らないHBlank（水平帰線期間））が始まります。
    この休憩時間の間に、PPUは先ほど見つけ出した「最大8個のキャラクター」それぞれの細かい設定を読み込み、**「CHR-ROMからグラフィック（下位・上位ビット）を拾う」** という、背景と同じようなフェッチ作業を8回（8体分）繰り返して、内部のシフトレジスタ（待ち行列）にセットします。

---

## 4. すべてが交わる場所：「ピクセル出力シフトレジスタ」

ここまでで、背景のためのデータ（次々と供給される8ドットごとの塊）と、スプライトのためのデータ（最大8個分の塊）が、それぞれPPU内部で無事に用意されました。

いよいよ最終段階、これを**「1ピクセルずつテレビに色として撃ち出す（出力する）」** 仕組みです。
PPUの内部には、「16ドット分の色情報」を横に並べて保持できる、巨大な**「シフトレジスタ（ベルトコンベアー）」**が存在します。

### バックグラウンド（背景）のベルトコンベアー
前回学んだ「8歩のフェッチ（N,A,L,H）」が終わるたびに、PPUは拾ってきた8ドット分の新しい色データを、この巨大なベルトコンベアーの右端に「ドン！」と並べて置きます。
そして、クロックが1進む（画面のドットが右へ1つ進む）たびに、ベルトコンベアー全体がカチャッと「左へ1ドット分シフト」します。
結果として、**コンベアーの左端から1ドット（ピクセル）ずつ、色情報がポロポロとこぼれ落ちてきます**。これが、現在テレビの画面に映るべき背景の「点（ドット）」の色になります。

### ファインXスクロール（少しのズレ）
A章で学んだ、スクロールの微調整用の `$2005` の `Fine X`（0〜7ドット）を覚えているでしょうか。
実はあれの正体は、この「ベルトコンベアーの色が左からポロポロこぼれてくる」時に、**「最初の何ドット分（0〜7ピクセル）を捨てて捨象してしまうか（マルチプレクサをズラすか）」を選択するだけのバルブ** の役割を持っているのです。
8マスの大きなマス目（Coarse X）を超えた「1文字未満の細かい横スクロールのヌルヌルした動き」は、このハードウェアの小さなバルブ（Fine X）の切り替えだけで見事に実現されています。

### 合成（ミキサー）とカラーパレット
こぼれ落ちてきた「背景の1ドット」と、裏で用意されていた「スプライトの1ドット（現在位置Xに一致した場合のみ出てくる）」の2つの色が揃いました。
PPUの出力回路（マルチプレクサと優先度決定器）は、以下の単純なルール（優先順位）でどちらの色をテレビに送るかを一瞬で判定します。

1. **両方の色が「透明色（パレットIDが0）」なら**: テレビには共通の背景色（大空の青色など）を出力。
2. **スプライトだけが「透明色ではない」なら**: そのスプライトの色（マリオの赤色など）を出力。
3. **背景だけが「透明色ではない」なら**: その背景の色（レンガの茶色など）を出力。
4. **両方とも重なって「色がついている」なら**:
   - スプライト側の属性（Attribute）にある「背景を背後（Priority）にする設定」がONなら、背景の色を勝たせる。
   - そうでなければ、スプライト（マリオ）が前に出て、背景（土管）が上に隠れる！

こうして選ばれた最終的な1つの「16進数のパレット番号(0x00〜0x3F)」が、エミュレータ（Rust）からGUI（winit/pixelsな）のカラーRGBマップ（255, 0, 0 等）に変換され、ついにあなたのパソコンの画面上にある、たった1個のピクセルとして美しく点灯するのです。

---

## 5. 最後に：完璧なファミコンを目指して

「A：スクロールのLoopyレジスタ」
「B：VRAMミラーリングの配線ハック」
「C：継ぎ接ぎされたiNESファイルの解剖」
「D：サブルーチンを支えるスタックの積み皿」
「E：PPUの8歩と精密なベルトコンベアーの合成」

今回追加されたこれら5つ（10ファイル）の高度な知識と、最初の1章〜9章の基礎を完全に理解・実装できれば、あなたの作るファミコンエミュレータは、ドンキーコングのようなシンプルなゲームから始まり、徐々にドラクエのようなRPGや、マリオブラザーズのようなスクロール・アクションゲームまで、**世の中に存在するありとあらゆる名作ROMファイルを、寸分の狂いもなく完全に動作させる「究極のマスターピース」** に到達することができます。

エミュレータ開発の最大の醍醐味は、この無機質な1と0の計算ロジック（Rustのコード行の羅列）が組み合わさって、突如として幼き日の「あの音楽」「あのグラフィック」「あの手触り」を現代のデジタル世界に完璧に蘇らせる瞬間の感動にあります。

ぜひ、恐れることなくこの先へと進み、あなただけの完璧なマリオを歩かせてみてください！
