# NESエミュレータ開発のはじめかた（8-2：Rustでのマッパー実装とMMC1）

前回（8-1）で、ファミコンのカセットの中には「マッパー」という独自のチップ（小人）が物理的に組み込まれており、CPUからの司令（書き込み）を受け取って32KBの窓から見えるページ（バンク）をガチャンと切り替えていることを学びました。

今回は、この「窓をスライドさせる小人」の実体を、エミュレータ全体を管理するRustのコードでどうやって美しく表現していくのかを見ていきます。

---

## 4. Rustでのマッパ（Mapper）の実装イメージ

エミュレータを作る際、カセット（ROM）のデータをカプセル化する `Cartridge` クラスの中に、この「マッパー」の機能を実装します。
ゲームのロムヘッダ（iNESフォーマットなど）の先頭16バイトには「このゲームはマッパー〇番を使って動かしてね」という指定が必ず書かれています。

そこで、全く違う挙動をする多数のマッパーたちを、エミュレータのバス（Bus）から共通のやり方で呼び出せるように、Rustの **Trait（トレイト：インターフェース機能）** を使って形を決めます。

```rust
// 色々なマッパーを、CPUやPPUから共通で扱うためのルール(Trait)
pub trait Mapper {
    // 1. CPU(またはPPU)が指定番地を「読みたい」と言ってきたとき、
    //    「現在のページ設定なら、カセット内の物理的な大容量データのどの場所(0〜255KB)を見せればいいか」
    //    を計算して返す関数。
    fn cpu_map_read(&self, addr: u16) -> Option<usize>;
    
    // 2. CPUが指定番地に「書き込んだ」とき、マッパーの小人がそれを受け取って
    //    自分の内部状態（ページ番号や画面の反転設定など）を変更する関数。
    //    (マッパーが処理を自作自演で横取りしたなら true を返す)
    fn cpu_map_write(&mut self, addr: u16, data: u8) -> bool;
    
    // （※実際にはこれに加えて、PPU用の chr_map_read / write も必要です）
}
```

このルールに従って、「マッパー0用」「マッパー1用」「マッパー4用」の部品をそれぞれゲームごとに切り替えてカセットに装着（インスタンス化）してあげる構造になります。

---

## 5. 例：一番有名な「マッパー1 (MMC1)」の仕組み

スーパーマリオブラザーズやパックマンなどの初期ゲーム（マッパー0）の実装は、ただ渡されたアドレスをそのまま返すだけのスルー状態（ページ切り替え無し）なので、非常に簡単です。

ここではエミュレータ開発者が最初にぶつかる壁であり、ゼルダの伝説などの名作を支えた「マッパー1 (MMC1)」の仕組みをのぞいてみましょう。

### なぜか「1ビットずつ」送る特殊な通信

MMC1マッパーは、CPUから「書き込み」を受け取ってページをめくります。
しかし、マッパーチップ自体も限界までコストダウンで作られているため、「1回の書き込みで8ビット（1バイト）全体を受け取る窓口」がありません。その代わりに、「1回の書き込みで1ビットしか受け取れない」という特殊な仕様（シフトレジスタの一種）を持っています。

そのためゲーム側のCPUは、`0x8000` 番地に向かって「今のページ番号の最下位1ビットだけを送る」×「5回ルード」 を繰り返して、やっと新しい5ビット分（最大32ページの切り替え）のバンク番号をマッパーの小人に伝えることができるのです。

```rust
// Mapper 1 (MMC1) の実装イメージ
pub struct Mapper1 {
    shift_register: u8, // CPUから1回ごとに送られてくる1ビットをチマチマ貯める箱
    
    // 5回分貯まったら、以下のどちらかの設定に上書きされる
    prg_bank: u8,       // 現在選ばれているPRG-ROM(プログラム)のバンク番号
    chr_bank: u8,       // 現在選ばれているCHR-ROM(グラフィック)のバンク番号
    
    // (他にも画面の縦横スクロール方向を決めるミラーリング設定などがあります)
}

impl Mapper for Mapper1 {
    fn cpu_map_write(&mut self, addr: u16, data: u8) -> bool {
        // CPUが $8000 以上（本来書き込めないROMの場所）に書き込んだ！
        if addr >= 0x8000 {
            // (リセット信号などが来た場合の処理は一旦割愛します)

            // 1. 送られてきたデータの一番下(1ビット)だけを切り取り、シフトレジスタ(貯金箱)に押し込む
            self.shift_register >>= 1;
            self.shift_register |= (data & 1) << 4;

            // 2. もしシフトレジスタが5回分(5ビット分)パンパンに満たされたら、設定を決定する！
            // (どこに書き込まれたかのアドレス帯域によって、何の切り替えかが決まる)
            if /* 5回書き込まれたかの判定ロジック */ {
                // $E000 以上に書かれていたら、PRGページ(プログラム)の切り替え指示だ！
                if addr >= 0xE000 {
                    self.prg_bank = self.shift_register & 0x0F;
                }
                
                // 決定後、マッパーの小人はシフトレジスタを空に戻して次の通信を待つ
                self.shift_register = 0x10; 
            }
            return true; // マッパーが処理を無事に完了したよ、とCPUに返す
        }
        false
    }
    
    // -------------------------------------------------------------
    
    // CPUがプログラムを「読みたい（Read）」と言ってきた時の変換処理
    fn cpu_map_read(&self, addr: u16) -> Option<usize> {
        // CPUが前半の窓($8000~$BFFF)を覗いた場合
        if addr >= 0x8000 && addr <= 0xBFFF {
            // 今マッパーで選ばれているバンク(prg_bank)を見せる！
            // (例： バンク3(16KB = 16384) が選ばれていたら、大元データの 49152 番地以降を返す)
            return Some((self.prg_bank as usize * 16384) + (addr & 0x3FFF) as usize);
        }
        
        // CPUが後半の窓($C000~$FFFF)を覗いた場合
        if addr >= 0xC000 && addr <= 0xFFFF {
            // 後半の窓は、トラブル防止の「固定ページ（ゲームの最後のバンク）」を常に見せる仕様等がある
            // ...固定ページのアドレス計算をして返す...
        }
        None
    }
}
```

このように、マッパーの実装とは**「読み取りのアドレスを、現在選択されているページ番号に合わせてズラす（オフセットを足す）数学の計算式（マッピング）を書くこと」**に他なりません。
（アドレスをマップするから「マッパー」なのです）

---

## 6. これであなたもエミュレータ開発者です

「本来書き込めないROMに対して、あえて『書き込み』をすることで、特殊なチップに裏から指示を出す」
「線が足りないから、1ビットずつ5回に分けて時間をかけて通信する」

このハック的で泥臭く、しかし洗練されたハードウェアの工夫こそが、ファミコンという限界だらけの機械で名作RPGや巨大なアクションゲームを生み出すことができた「先人たちの魔法の集大成」です。

CPU、PPU、時間同期、コントローラ入力、そして今回の大容量化の魔法（マッパー）。
ここまでくれば、エミュレータを形作る機能はほぼすべて理解したことになります。あとは自分の好きなゲーム（マッパー番号）に対応したロジックを次々と書いていくだけで、どんなファミコンソフトも現代のパソコンに呼び覚ますことができるようになります。

最後の一つ（その9）として、ファミコンのもう一つの魂である「音源（APU）」がどうやって鳴っているのかを解説して、この長い旅を締めくくりましょう。
