# NESエミュレータ開発のはじめかた（5-1：カセットの秘密とPPUのキャンバス）

今までCPUの気持ちになって、「メモリから数字を取って足し算する」「フラグを見てジャンプする」といった頭脳（ロジック）の部分のみを実装してきました。
しかし、どんなにスーパーマリオの座標計算を正確に行ったとしても、ファミコンはテレビに映像が映らなければただの暖房器具です。

ここからは、計算された座標やスコアをどのようにして色鮮やかな映像に変換しているのか、**「PPU（Picture Processing Unit）」** という専用のグラフィックチップと、データの大元である**「カセット（ROM）」**の密接な関係について解説します。

---

## 1. カセット（ROM）の中には何が入っているのか？

子供の頃、ふーふー息を吹きかけてから本体に挿していた「ファミコンのカセット」。
実はあのプラスチックの箱の中には、基板と一緒に**2種類の全く役割が違う記憶チップ（ROM）**が搭載されています。

1. **PRG-ROM (Program ROM)**: 今まで見てきた、CPUからの「司令書（オペコード）」の塊です。マリオのジャンプの高さや、敵の動きのアルゴリズムなど、「ゲームのルール」が書かれています。
2. **CHR-ROM (Character ROM)**: 画面に表示する「画像の元データ（ドット絵のパーツ）」の塊です。

### 🎨 CHR-ROM：すべての元となる「スタンプカード」

現在のゲーム開発システム（UnityやUnreal Engineなど）とは異なり、ファミコンのプログラム自体（PRG-ROM）には「マリオの顔の形を線で描け」といったプログラムコードは一切書かれていません。

その代わり、カセットの中（CHR-ROM）には、あらかじめ **8×8ピクセルの小さなドット絵のスタンプ（タイル）** が、何百個もぎっしりと敷き詰めて保存されています。これらスタンプのセットを「パターンテーブル」と呼びます。

```text
[ CHR-ROM (パターンテーブル) のイメージ ]

+--------+--------+--------+--------+
| 0番    | 1番    | 2番    | 3番    |
| (Aの字)| (レンガ)| (雲の右)| (コイン)|
+--------+--------+--------+--------+
| 4番    | 5番    | 6番    | 7番    |
| (マリオ| (の頭部)| (クリボ| ーの足)|
+--------+--------+--------+--------+
```

ここで重要な設計ルールがあります。
あの万能に見える**CPUは、この「絵柄のデータ（CHR-ROM）」を直接読むことはできません**。（※一部のマッパーを用いた特殊な例外を除きます）
このスタンプ箱の中身を直接覗いて、テレビ画面の規格に合わせてスキャンすることができるのは、**映像職人である PPU だけ** なのです。

---

## 2. PPU（グラフィックチップ）の仕事場「VRAM」

PPUは、テレビに対して「今、電子ビームが通っている場所にはこの色を映せ！」と毎秒60回という猛烈なスピードで送り続ける、ファミコンのグラフィックに特化した専門の職人です。

この職人が仕事をするための専用の作業机（メモリ）があります。それが **VRAM (Video RAM)** です。
（CPUが持っていた `0x0000〜0xFFFF` のメモ帳とは全く違う、PPU専用の独立した空間です）

### 🗺 ネームテーブル：スタンプを押す巨大な「設計図」

先ほどの「CHR-ROMにあるスタンプ」を組み合わせてスーパーマリオの広大な1画面を作るため、PPUの作業机（VRAM）には **ネームテーブル（Name Table）** という「画面の設計図」を置くための領域が存在します。

これは、テレビ画面を「横32マス × 縦30マス（合計960マス）」の格子状に細かく区切った表です。

```text
[ ネームテーブル (画面の設計図: 32x30のマス目) ]

  00 01 02 03 04 ... 31 (列)
00 [ ][ ][ ][ ][ ]...[ ]
01 [ ][B][A][C][ ]...[ ]
02 [ ][1][ ][2][ ]...[ ]
03 [ ][ ][ ][ ][ ]...[ ]
...
29 [ ][ ][ ][ ][ ]...[ ]
(行)
```

この各マスには、**「何番のスタンプを押すか（0〜255）」** という番号（インデックス）だけが書かれています。

絵そのもののデータ（「1マスあたり2ビット×64ピクセル」の複雑な色データ）はネームテーブルには書かれていません。
代わりに「ここは1番のスタンプ！（レンガ）」「ここは3番のスタンプ！（コイン）」という番号だけをパラパラと書いておきます。

すると、描画職人であるPPUがテレビ画面を上から下へ描画していく（走査線を進める）過程で、自動的に「ネームテーブルを見る」→「指定された番号のスタンプをCHR-ROMから見つけてくる」→「パレットの色に合わせて画面にドットを打つ」という信じられないほど複雑な作業を、ハードウェアの力で一瞬でこなしてくれるのです。

### 背景と「属性（Attribute）」

ネームテーブルで形を決めるだけでは、昔のゲームボーイのような白黒（あるいは単色）の画面にしかなりません。
カラフルな絵にするために、VRAMにはもう一つ **「属性テーブル（Attribute Table）」** という不思議なパレット設定領域があります。

ネームテーブルの後ろにひっそりと置かれたこの領域には、「画面の『右上の一角（16x16ピクセルブロック）』のスタンプたちは、赤色系のパレットを使え」「左下の一角は青色系のパレットを使え」という**大雑把な色の指定**が書かれています。
（ファミコンの独特な「ブロックごとに色がカクカク分かれている」見た目は、この属性テーブルという容量を極限まで節約した設計の賜物なのです）

---

このように、「カセットにあるスタンプ（CHR-ROM）」と「PPUの手元にある設計図（VRAM・ネームテーブル）」が組み合わさることで、初めてファミコンの「背景」が完成します。

しかし、ここで最大の疑問が湧きます。
「ネームテーブルがあるのは分かったけど、**計算を行っている主人公（CPU）は、どうやってその『PPU専用の机（VRAM）』にスタンプの並び順を書き込むの？**」

次章（5-2）では、物理的に切り離されたCPUとPPUがどうやって会話をしているのか、ファミコンのエミュレータ開発で最も美しいとされる「PPUポートとOAM」の通信メカニズムについて解説します。
