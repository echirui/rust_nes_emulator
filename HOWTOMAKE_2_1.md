# NESエミュレータ開発のはじめかた（2-1：レジスタの深掘りとアドレッシングモードの概念）

前回（1-2）で、「アキュムレータ（Aレジスタ）に数字を直接入れる」という最も簡単な `LDA` 命令（即値）を作りました。
しかし 본격的に開発を進める前に、CPUの中にある「レジスタ（引き出し）」たちについて、より深い直感的なイメージを持っておく必要があります。

なぜなら、ファミコンのCPUにとって「扱う数字（データ）」は、手元の引き出しにあるのか、それとも広大なメモリ空間のどこかにあるのか、その**「探し方」**こそがプログラミングの肝になるからです。

---

## 1. 登場人物（レジスタ）のイメージ図と詳細

ファミコンの CPU (6502) は非常にシンプルで、プログラマが直接操作できる「引き出し」は **たったの6つ** しかありません。

```text
       [ CPU の引き出し（レジスタ） ]
       
+-------------------+----------------------------+
| 📦 A (アキュムレータ) | 主役。計算や移動の中心になる箱。   |
+-------------------+----------------------------+
| 📦 X (インデックス)  | カウンタや、配列の「n番目」用。   |
+-------------------+----------------------------+
| 📦 Y (インデックス)  | Xと同じく「n番目」として活躍。   |
+-------------------+----------------------------+
| 📝 P (ステータス)    | 「ゼロだったフラグ」等のメモ帳。   |
+-------------------+----------------------------+
| 📚 SP (スタック)     | 本棚（メモリ）の「今の高さ」記録。|
+-------------------+----------------------------+
| 🎯 PC (プログラム)   | 次に実行する「命令集の行数」表記。|
+-------------------+----------------------------+
```

### 📦 A，X，Y レジスタ (8ビット: 0〜255)
これらは**「数字を手元に置いておくための小箱」**です。
*   **A (アキュムレータ)**: 全ての計算の「中心」です。足し算も引き算も、基本的に「Aレジスタの中身」に対して行われ、結果も「Aレジスタ」に上書きされます。そのため非常に出番が多い引き出しです。
*   **X, Y (インデックスレジスタ)**: 主に「ループ処理（10回繰り返す等のカウンタ）」や「メモリの〇番目」というようにオフセット（ズレ）を指定するために使われます。例えばスーパーマリオのプログラムで「3体のクリボーの座標を順番に処理する」ときに、「今X体目のクリボーを処理中」といった具合に使われます。

### 📝 P レジスタ (ステータスフラグ / 8ビット)
計算結果の**「感想」を書き留める自動メモ帳**です。
前回、Aに `0` を入れた直後、CPUはこのPレジスタに「あ！今入れた数字、ゼロだったよ！（Zフラグを1にする）」と自動で記録しました。他にも「マイナス（負）の数になったよ！(Nフラグ)」「足し算したら255を超えて繰り上がったよ！(Cフラグ)」など、計算の直後にその状態をメモします。このメモを見て、あとで「もしゼロだったらジャンプしろ」などの判断を下します。

### 🎯 PC と 📚 SP (16ビット と 8ビット)
*   **PC (プログラムカウンタ)**: CPUが「今、辞書（メモリ）の何ページ目を読んでいるか」を指差している「しおり」です。ジャンプ命令が来ると、この「しおり」の場所がガバッと飛ぶことで、プログラムの別の場所が実行されます。
*   **SP (スタックポインタ)**: 特殊なメモリ領域（後で見返すためにデータを山積みに重ねて保管する場所）の「今、一番上の段はどこか」を覚えている箱です。

この6つの引き出しを器用に操作するのが、これから実装していく命令（オペコード）の全ての目的です。

---

## 2. 1つの「LDA」から広がる世界：アドレッシングモード

さて、役者（レジスタ）がすっかり頭に入ったところで、いよいよ前回作った `LDA` 命令の「真の姿」に迫りましょう。

前回は `0xA9` というオペコードを使って「すぐ後ろに書いてある数字をAレジスタに入れなさい」という命令を作りました。
しかし、例えばRPGで「プレイヤーのHP（ヒットポイント）」をロードしたいときはどうでしょうか？HPは常に変わるため、固定の数字（42など）ではなく、メモリのどこかに保存されていなければなりません。

そこで、「Aレジスタに入れる」という目的は同じでも、**「数字をどこから見つけてくるか（探し方）」** を変える必要が出てきます。
この「探し方のルール」のことを **アドレッシングモード（Addressing Modes）** と呼びます。

### 代表的なアドレッシングモード（基礎編）

#### ① イミディエイトモード (Immediate / 即値)
前回実装したモードです。「すぐ次の番地に書いてある数字」をそのまま使います。
固定の初期値（ライフの最大値など）をセットするときに使われます。

```text
命令: 0xA9 (LDA)
値:   42 (0x2A)
行動: Aレジスタにはそのまま「42」という数字が入る。
```

#### ② ゼロページモード (Zero Page)
メモリの `$0000` から `$00FF` の範囲は**「ゼロページ」**と呼ばれ、ファミコンの中で最も速く、かつ命令の容量を食わずにアクセスできる「手元の引き出し」のような特別な領域です。ファミコンの変数はよくここに置かれます。

```text
命令: 0xA5 (LDA)
番地: 10 (0x0A)
行動: メモリの「10番地」に入っている数字を見に行き、その中身をAレジスタに入れる。
```
※即値（そのまま）と違い、一旦メモリの中身を覗きに行く「ひと手間」がかかります。

#### ③ アブソリュートモード (Absolute / 絶対番地)
ゼロページ以外の広大なメモリ（`$0100`〜`$FFFF`）のどこかにあるデータを、住所（16ビットのアドレス）をフルに指定して取りに行くモードです。

```text
命令: 0xAD (LDA)
番地の下桁: 0x34
番時の上桁: 0x12
行動: メモリの「0x1234番地」まで遠出をして数字を見に行き、Aレジスタに入れる。
```
※住所が2バイト分（下桁と上桁）必要になるため、一番メモリの容量を食い、実行にも時間がかかります。

---

このように、**「Aレジスタに入れる（LDA）」というやりたい事（目的）は同じなのに、「どうやってそのデータを探すか（手段）」によって命令の番号（オペコード）が変わる** というのが、6502CPUの最大の特徴であり、エミュレータ開発者が最初にぶつかる壁です。

次章（2-2）では、この多岐にわたるアドレッシングモードを、Rustのスマートな機能を使って1つの関数に綺麗にまとめる「魔法の実装方法」について解説します。
