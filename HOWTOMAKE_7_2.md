# NESエミュレータ開発のはじめかた（7-2：コードで繋ぐプレイヤーとマリオ）

前回（7-1）で、ファミコンのコントローラは「8個のボタン状態（1バイト）」を、1本の通信線を使って「1回に1個（1ビット）ずつ順番に返す」という、コスト削減のための面白いシフトレジスタの仕組みを持っていることを学びました。

今回は、この「1ビットずつポロッと出てくる」ギミックを、エミュレータ側（Rust）でどうやって実装し、最終的にパソコンのキーボード入力と繋ぎ合わせるかを見ていきます。

---

## 3. Rustでのコントローラ（Joypad）の実装

CPUから見ると、「`$4016` に `1` を書き込み、また `0` を書く（ストロボ/ラッチ）」ことで準備をし、「`$4016` を何度も読む」ことで順番にデータを引き出します。

この動作をエミュレータ側で表現するために、プログラムの中に `Joypad` という状態を管理する小さな箱（構造体）を作りましょう。

```rust
pub struct Joypad {
    // 1が書き込まれた状態(ストロボがON)かどうか。
    // この間はボタンの並び順がリセットされ、常に「Aボタン」の場所を指し続ける。
    pub strobe: bool,      
    
    // 次に読まれたとき「どのボタンの情報を返すか」という順番のカウンター(0〜7)
    pub button_index: u8,  
    
    // 現在のフロントエンド（PCキーボードなどから渡された）1バイト(8ビット)の最新ボタン状態
    pub button_status: u8, 
}

impl Joypad {
    pub fn new() -> Self {
        Joypad {
            strobe: false,
            button_index: 0,
            button_status: 0,
        }
    }

    // パソコンのキーボードなどから、実際の最新ボタン状態(0〜255)を毎フレームセットする関数
    pub fn set_buttons(&mut self, status: u8) {
        self.button_status = status;
    }

    // CPUが $4016 に「書き込み」を行ったときの処理 (ストロボ動作)
    pub fn write(&mut self, data: u8) {
        // もし送られてきたデータの最下位ビットが 1 なら、ストロボ ON。
        // 0 ならストロボ OFF (ラッチ完了、写真固定)。
        self.strobe = (data & 1) == 1;
        
        if self.strobe {
            // ストロボがONの間は、何度読まれても常に最初の「Aボタン(0番目)」を指し続ける
            self.button_index = 0;
        }
    }

    // CPUが $4016 から「読み取り」を行ったときの処理
    pub fn read(&mut self) -> u8 {
        if self.button_index > 7 {
            // もしゲームプログラムがバグっていて、8回を超えて読まれてしまったら
            // とりあえず常に「1」を返すのが実機の挙動（配線がプルアップされているため）
            return 1;
        }

        // 1. 現在のカウンター(0〜7)が指すボタンのビットだけを 1バイトの中から取り出す。
        // （例：右シフトして、一番右(0ビット目)を & 1 で綺麗に切り抜いて返す）
        let response = (self.button_status & (1 << self.button_index)) >> self.button_index;

        // 2. ストロボがOFF(固定中)の時だけ、次に備えてカウンターを1つ進める。
        // これによって、次の read() では隣のボタン情報が読めるようになる！
        if !self.strobe && self.button_index <= 7 {
            self.button_index += 1;
        }

        response
    }
}
```

これを `bus.rs` などのメモリ全体を管理する場所（Memory Mapped I/O の中心地）の中に組み込みます。

```rust
// Busの中の読み書き処理 (Memory Map) の追加イメージ
impl Bus {
    pub fn mem_read(&mut self, addr: u16) -> u8 {
        match addr {
            // もしCPUが $4016 を読みたいと言ってきたら、Joypad の read() が返す値を渡す
            0x4016 => self.joypad1.read(),
            // ... (PPUなど他の処理)
        }
    }

    pub fn mem_write(&mut self, addr: u16, data: u8) {
        match addr {
            // もしCPUが $4016 に書き込みたいと言ってきたら、Joypad の write() にデータを流す
            0x4016 => self.joypad1.write(data),
            // ...
        }
    }
}
```

これで、CPU側は「面倒な8回ループ」をいつも通り実行するだけで、エミュレータ側の `Joypad` クラスが賢く1ビットずつ情報を返してくれるようになりました。

---

## 4. フロントエンド（PCキーボード）との接続

さあ、最後の仕上げとして、エミュレータの一番外側にある「フロントエンド（`main.rs`）」に戻ってきます。

PPUが描画した絵をウィンドウに出すのと同じように、今度は `winit` などのウィンドウライブラリを使って、**「ユーザーが今キーボードのどのキーを押しているか」という現実世界の入力** を監視します。

たとえば、「Xキーが押されたらAボタン扱いにする」「矢印キーが押されたら十字キー扱いにする」といったように、キーボードの入力状態を監視し、それを **1バイト（8ビット）のデータ（`0x00`〜`0xFF`）に変換** して、先ほどの `set_buttons()` 関数に毎フレーム（1秒間に60回）渡してあげます。

```rust
// main.rs のメインループ内のイメージ
event_loop.run(move |event, _, control_flow| {
    
    // 1. 現実のキーボード入力を受け取り、1バイトの `controller_state` を作る。
    // （例： もしXキーが押されていたら、Aボタンの場所(bit7)を 1 に立てる(0x80) など）
    let mut controller_state = 0;
    if keyboard.is_pressed(KeyCode::X) { controller_state |= 0x80; } // A
    if keyboard.is_pressed(KeyCode::Z) { controller_state |= 0x40; } // B
    if keyboard.is_pressed(KeyCode::Right) { controller_state |= 0x01; } // R
    // ...
    
    // 2. エミュレータの中のジョイパッドに「今の最新状態だよ」と教える
    nes.input(controller_state); // nes.bus.joypad1.set_buttons(..) を呼び出す

    // 3. エミュレータを1フレーム(1/60秒分)進める (CPUやPPUが物凄い回数動く)
    nes.update();
    
    // 4. 完成した映像をウィンドウに描き、音を出す
    // ... （繰り返す）
});
```

---

## 5. 完成！初めての「自分で操作できるゲーム」

これで、キーボードを押すたびにその情報がエミュレータ内部の `Joypad` 変数にセットされるようになりました。そしてゲーム内のCPUが、毎フレームの NMI のタイミングで `$4016` を8回読みとるループを回した際に、正しく「あ！今、右が押されているぞ！」と判断できるようになります。

この瞬間、あなたがこのエミュレータ開発のはじめかた（その1）から少しずつゼロから組み上げてきた『たった一つの単純な足し算（LDAなどのCPU実装）』という歯車が、PPU という映像クリエイターと協力し、あなたが与えたコントローラの入力に反応して**「マリオが右へ歩き出す」**のです。

プログラムにおいて、「自分の作った仮想の脳が、自分の入力を受けて現実のように動く」という、これ以上感動する瞬間はなかなかありません。
ぜひ、この興奮を忘れないうちに、次章の「特殊なカセット（マッパー）」による大容量ゲーム対応の世界へと進みましょう！
