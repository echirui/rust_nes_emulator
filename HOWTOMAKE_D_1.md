# NESエミュレータ開発の壁（D-1：机の端っこのメモ帳「スタック」）

ここまでのプログラミング（その4など）で、CPUは「メモ帳（メモリ）から数字を読み取って、足し算をして、結果をしまう」という単調な作業を繰り返していることを学びました。

しかし、ゲームや現代の大規模なプログラムを書く上で絶対に欠かせない機能があります。それは**「ちょっと今の作業を中断して、別の仕事（関数）にワープする。終わったら、さっき中断した場所に戻ってくる」** というサブルーチン（関数呼び出し）の技術です。

このタイムトラベルのような機能を実現するために、ファミコンのCPU(6502)がハードウェアとして内蔵している特別な機能、**「スタック（Stack）」** の仕組みについて解説します。

---

## 1. 散らかる机と「スタック」という考え方

CPUが現在持っている「引き出し（レジスタ）」は、A、X、Y の3つしかありません。
もし、ある計算の途中で「急に敵が現れたから、敵のダメージ計算を先にやらなきゃ！」となった時、Aレジスタに入っていた大切な「今のマリオのスコア」はどうやって保存しておけばいいのでしょうか？

もちろん、メモリのどこかの番地（例： `$0020` 番地）を「マリオのスコア保存用」として予め決めておいて、そこに書き込む（STA）こともできます。しかし、処理が複雑になるにつれて「誰がどこの番地を使っているか」をいちいちプログラマが指定して管理するのは非常に面倒ですし、バグ（上書き事故）の原因になります。

### 皿を積み上げる「スタック」

そこで考え出されたのが、**「LIFO（Last In, First Out：後入れ先出し）」** というデータ構造です。
カフェのカウンターに積まれた「お皿」を想像してください。

1. **プッシュ（Push）:** 新しいお皿（データ）は、一番**上**にポイッと乗せる。
2. **ポップ（Pop）:** お皿（データ）を使いたい時は、一番**上**から取り出す。

これなら、「とりあえず今のデータ（マリオのスコア）を、一番上にポイッと仮置きしておけ」「用事が終わったら、一番上から1個取り戻せば、元通りに戻る」というように、**番地をいちいち指定しなくても、自動で順序正しくデータを保存・復帰できる** のです。

---

## 2. ファミコンの「スタック領域」と「SPレジスタ」

ファミコンのCPUは、この「お皿を積む便利機能（スタック）」をサポートするために、メモリ空間の **`$0100` から `$01FF` までの 256バイト** を「スタック専用のメモ帳（ページ1）」としてハードウェア的に固定（優遇）して割り当てています。

さらに、その2にあるCPUのレジスタ一覧で、1つだけ説明を省いていた引き出しがありました。
それが **「SP（Stack Pointer：スタックポインタ）」** レジスタです。

SPは、「現在、スタック（山積みになったお皿）の一番上の高さは、どこまで来ているか？」という **番地の下2ケタ(`0x00`〜`0xFF`)のみ** を指し示す専用のポインタです。

### プッシュ（PHA）とポップ（PLA）の動き

ファミコンには、一時的にレジスタの数字をお皿に積む専用の命令 `PHA (Push Accumulator)` があります。
`PHA` が実行されると、以下の自動作業がCPUの内部で行われます。

```text
[ 初期状態: SP = 0xFD (お皿の高さは $01FD を指している) ]

1. CPUは、Aレジスタの数字(例: 42)を、現在の高さである `$01FD` に書き込む。
2. その直後、SPの数字を「1つ減らす」（SP = 0xFCにする）。
   ※お皿を積むと、残りの空きスペースが減る（上に伸びる）ので、計算上マイナスされます。
   
[ 実行後: $01FDには42が保存され、次回のお皿を置く場所(SP)は $01FC になった！ ]
```

逆に、作業が終わってデータを取り戻したい時は `PLA (Pop Accumulator)` を呼びます。

```text
[ 今、SPは $01FC を指している ]

1. 先取りして、SPの数字を「1つ増やす」（SP = 0xFD に戻す）。
2. `$01FD` に書かれている数字(42)を読み取り、Aレジスタに戻す。

[ 実行後: Aレジスタは見事に 42 に復帰した！ ]
```

これを繰り返すことで、例えば `PHA` -> `PHX` -> `PHY` と3つ積んだ後、逆に `PLY` -> `PLX` -> `PLA` と取り出すことで、すべてのレジスタの中断前の状態を無傷で完全に復元できるのです。（※その6のNMIの解説で唐突に出た「スタックに一時退避する」というのは、まさにこの `PHA` などの機能のことです）

---

## 3. 「しおり」ごとスタックに積むという大発明

ただ数字（データ）を積むだけなら、便利な一時メモリに過ぎません。
しかし、この仕組みを応用して**「今読んでいるプログラムの行番号（PC: プログラムカウンタ）」自体をスタックに積んでワープする** という大技を可能にしたのが、近代プログラミングの基礎である**「サブルーチン（関数の呼び出し）」**です。

次章（D-2）では、この「しおり」をスタックに退避させて別世界へワープする命令である `JSR` と、そこから生還する `RTS` 命令の美しいコンビネーションについて、Rustでの実装を交えて解説します。
