# NESエミュレータ開発のはじめかた（1-2：はじめての命令とテスト駆動開発）

前章で、プログラミングの中に「引き出し（レジスタ）」をいくつか持っただけのまっさらなCPUを生み出しました。
今回は、このCPUに「命令」を読み込ませて、実際に引き出しの中の数字が変化する様子を観察します。

エミュレータ開発において最も楽しく、そしてパズルを解いていくような感覚が味わえるのが、この「命令の実装」フェーズです。

---

## 3. CPUの呼吸：Fetch - Decode - Execute

CPUは人間のように「あ、次はこれをやろう」と考えて動いているわけではありません。
ただひたすらに、以下の3つのステップを電源が切れるまで超高速で永遠に繰り返すだけの、極めて単調な機械です。

1. **フェッチ (Fetch / 読み取る)**: メモリの特定の番地から、次にやるべき「命令の番号（オペコード）」を1つ持ってくる。
2. **デコード (Decode / 解読する)**: 持ってきた番号を見て、「これは足し算だな」「これは移動だな」と理解（振り分け）する。
3. **エグゼキュート (Execute / 実行する)**: 解読した結果に従って、実際に自分のレジスタ（引き出し）の数字を変えたり、メモリに計算結果を書き戻す。

### 動作のイメージ図

```text
[ メモリ (ROM: カセットの中身) ]             [ CPU の中身 ]
+----------------------+
| 0xA9 (LDAの命令番号) | --------+
+----------------------+         ↓ (1. 読み取る/Fetch)
|  42  (読み込む値)    |       CPU「お、メモリから『0xA9』という数字が来たぞ！」
+----------------------+         ↓ (2. 解読する/Decode)
                               CPU「一覧表を見ると...これは『すぐ次の数字をAレジスタに入れろ』だな」
                                 ↓ (3. 実行する/Execute)
                               📦 Aレジスタ = 42 (完了！)
```

このように、「1バイトの命令番号」と、それに続く「処理するためのデータ（オペランド）」のセットが、CPUにとっての言葉になります。

---

## 4. 最初の命令を1つだけ憶えさせる（LDA命令）

ファミコンのCPUには数多くの命令がありますが、その中でも最も基礎となるのが、データを自分の中に読み込む命令です。
今回は **「LDA（Load Accumulator）」** という命令を1つだけ実装してみます。

*   **名前:** LDA (Load Accumulator)
*   **オペコード（命令番号）:** `0xA9` （即値というモードの場合）
*   **役割:** メモリから数字を読み取って、CPUの主役である `A` レジスタ（アキュムレータ）にそのままコピーする。
*   **副作用（ルール特典）:** 読み取った数字がもし `0` だったら、`P`レジスタ（メモ帳）に「ゼロだったよ！フラグ」を立てる。もし一番上のビット(負の数)が立っていたら「ネガティブフラグ」を立てる。

CPUが動くための `run_instruction` というお試し関数を作ります。

```rust
impl Cpu {
    // 1命令だけを実行するサンプル関数
    // 本来は本物のメモリ配列(`[u8; 65536]`)から読み取りますが、
    // 今日は簡単のために、「命令番号(opcode)」と「データ(data)」を直接渡してあげます。
    pub fn run_instruction(&mut self, opcode: u8, data: u8) {
        
        // 2. Decode (解読するフェーズ) : match文でどの命令かを判別する
        match opcode {
            
            // もし命令番号が「0xA9 (LDA)」だったなら...
            0xA9 => {
                
                // 3. Execute (実行するフェーズ)
                // ステップ1: 与えられたデータ(data)を、そのままAレジスタにロードする
                self.a = data;

                // ステップ2: 状態(ステータス)を更新する
                // もしロードした数字が 0 だったら、Pレジスタの 2ビット目(0x02) をONにする
                if self.a == 0 {
                    // 0b0000_0010 と「OR演算(|)」することで、そのビットだけを強制的に 1 にします
                    self.p = self.p | 0b0000_0010; 
                } else {
                    // 0b1111_1101 と「AND演算(&)」することで、そのビットだけを強制的に 0 にします
                    self.p = self.p & 0b1111_1101; 
                }

                // ネガティブフラグ（一番左の7ビット目が立っているか？）の更新
                if self.a & 0b1000_0000 != 0 {
                    self.p = self.p | 0b1000_0000;
                } else {
                    self.p = self.p & 0b0111_1111;
                }
            }
            // まだ知らない命令が来たらパニック（エラーで停止）させるか、無視します
            _ => {
                println!("知らない命令です: {:02X}", opcode);
            }
        }
    }
}
```

この「ビット演算（AND や OR）」は一見難しそうに見えますが、**「特定の位置にあるスイッチ（フラグ）だけを、他のスイッチに触らずにパチッと切り替えるためのテクニック」** に過ぎません。慣れれば非常に便利です。

---

## 5. `main` 関数で動かしてみよう！

役者は揃いました。`main` 関数でこのCPUを生み出し、 `0xA9 (LDA)` 命令を与えて、本当に `A` レジスタの内容が変わるか見てみましょう。

```rust
fn main() {
    println!("NES Emulator の開発スタート！");

    // 1. 新しいCPUを作る
    let mut cpu = Cpu::new();
    println!("初期状態: Aレジスタ = {}", cpu.a);

    // 2. CPUに「0xA9 (LDA命令) で、数番の『42』を読み込め」と命令を直接送り込む
    //    (Fetchの手順をショートカットしています)
    cpu.run_instruction(0xA9, 42);

    // 3. 結果を確認する！
    println!("実行後: Aレジスタ = {}", cpu.a);
}
```

ターミナルで `cargo run` を実行してみてください。

```text
NES Emulator の開発スタート！
初期状態: Aレジスタ = 0
実行後: Aレジスタ = 42
```

おめでとうございます！これが **自作のエミュレータが初めて命令を理解して、正しく動作した瞬間** です。

---

## 6. テスト駆動開発（TDD）という最強の武器

たった1つの命令でも `main` 関数で動かして `println!` で確認できたのは嬉しいですが、ファミコンのCPUには **56種類（細かな違いを入れると256通り！）** の命令が存在します。

これらを1つ作るたびに、いちいち `main` 関数を手で書き換えて `cargo run` を実行し、「あれ？Aレジスタの中は合ってるかな？」と目視で確認するのは、途方もない労力がかかりますし、何より**間違えの元**になります。

そこで、Rustに標準で備わっている **「自動テスト機能」** を使います。
これを「テスト駆動開発（TDD）」と呼び、エミュレータ開発において挫折しないための最強のテクニックとなります。

`main.rs` の一番下に、次のようなコードを追加してみてください。

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_lda_loads_data_into_a_register() {
        // 1. CPUを準備する
        let mut cpu = Cpu::new();
        
        // 2. LDA (0xA9) で 値 50 を読み込ませる
        cpu.run_instruction(0xA9, 50);

        // 3. Aレジスタが本当に「50」になっているか、Rustに自動でチェックしてもらう
        assert_eq!(cpu.a, 50);

        // 今回の数字は0ではないので、ゼロフラグは立っていないはず！
        // Pレジスタの中でゼロフラグ(下から2番目)だけを取り出し、それが0であることを確認する
        assert_eq!(cpu.p & 0b0000_0010, 0b0000_0000); 
    }

    #[test]
    fn test_lda_zero_flag() {
        let mut cpu = Cpu::new();
        // 0を読み込ませる
        cpu.run_instruction(0xA9, 0);

        assert_eq!(cpu.a, 0);

        // 0なので、ゼロフラグ(下から2ビット目)が立っているはず！(1になっているはず)
        assert_eq!(cpu.p & 0b0000_0010, 0b0000_0010); 
    }
}
```

### 自動テストの凄さ

このテストコードは、ターミナルで `cargo test` というコマンドを打つと一瞬で実行され、全て成功するか（間違った結果になっていないか）を教えてくれます。

```bash
$ cargo test

running 2 tests
test tests::test_lda_zero_flag ... ok
test tests::test_lda_loads_data_into_a_register ... ok

test result: ok. 2 passed; 0 failed...
```

一度このテストを書いておけば、もし数日後に「足し算（ADC）」の命令を書いているときに、うっかり「LDA」の仕組みを壊してしまっても、**すぐに `cargo test` がエラー（緑色ではなく赤色）を出して教えてくれるため、迷子にならずに安心して開発を続けることができます。**

**「命令を1つ書く」→「その命令のテストを書く」→「全テストが緑(ok)になるか確認する」**

この心地よいループこそが、巨大なファミコンのエミュレータを完成させるための唯一にして最短の道です。次は、このLDAが持つ「別のデータの探し方（アドレッシングモード）」という世界へと踏み込んでいきます。
