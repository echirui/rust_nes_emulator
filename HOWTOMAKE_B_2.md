# NESエミュレータ開発の壁（B-2：縦横無尽のスクロールとミラーリング実装）

前回（B-1）で、ファミコンのVRAMは2画面分しかなく、残り2画面は配線による「鏡写し（ミラーリング）」でごまかしていることを学びました。

今回は、ゲームのスクロール方向に合わせてカセットが配線をどう切り替えているのか、そしてそれをエミュレータ全体のメモリ管理システム（Rustコード）にどう組み込むのかを解説します。

---

## 3. 水平ミラーリングと垂直ミラーリング

ゲーム開発者は、自分が作るゲームの「スクロール方向」に合わせて、カセット基板上の「H（Horizontal）」か「V（Vertical）」の配線パッドをはんだ付けして出荷していました。

### ① 水平（Horizontal）ミラーリング

*   **誰が使う？:** 『アイスクライマー』や『エキサイトバイク』などの **「縦（上下）にスクロールするゲーム」** によく使われます。
*   **配置のルール:** 横に並んでいる部屋同士を、鏡（同じ実体）にします。すると、上下に異なる実体（VRAM）を配置できます。

```text
[ 水平ミラーリング時の 見え方 ]
  = 実体AとB (2KBのVRAM)

+-----------+-----------+
| 部屋0     | 部屋1     |   <= どちらも「実体 A」が見える
| (実体A)   | (実体A)   |
+-----------+-----------+
| 部屋2     | 部屋3     |   <= どちらも「実体 B」が見える
| (実体B)   | (実体B)   |
+-----------+-----------+
```
これなら、プレイヤーが上や下にスクロールしていくとき、別の実体（描画用の新しい背景）が待ち構えているので、スムーズに縦スクロールゲームを作ることができます。横方向には全く同じ景色（壁紙）がリピートしてスクロールします。

### ② 垂直（Vertical）ミラーリング

*   **誰が使う？:** 『スーパーマリオブラザーズ』などの **「横（左右）にスクロールするゲーム」** によく使われます。
*   **配置のルール:** 縦に並んでいる部屋同士を鏡にします。すると、左右に異なる実体（VRAM）を配置できます。

```text
[ 垂直ミラーリング時の 見え方 ]

+-----------+-----------+
| 部屋0     | 部屋1     |
| (実体A)   | (実体B)   |
+-----------+-----------+
| 部屋2     | 部屋3     |
| (実体A)   | (実体B)   |
+-----------+-----------+
```
これなら、マリオが右（部屋1）に向かって歩いていくとき、新しい実体Bの土管や背景がちゃんと待機しています。さらに右端を越えて進むと、今度は部屋0（実体A）側にループし、CPUが裏で実体Aを書き換えて新しい未来を構築するのです。

---

## 4. Rustでのミラーリング（アドレス変換）の実装

エミュレータでこの仕組みを表現するとは、**「PPUが『 `$2000`〜`$2FFF` のどこか（4部屋のどこか）』にアクセスしようとしたときに、それを『VRAMの実体配列（長さ2048バイト）の、0〜2047番目のどこか』へ数学的に変換（マッピング）してあげる関数の実装」** になります。

```rust
// カセット側が持つ列挙型（今のゲームはどっちの設定か？）
pub enum Mirroring {
    Vertical,
    Horizontal,
    // (※四画面や1画面ミラーリングなどの特殊なものもあります)
}

// 実際のPPU内の読み書き処理（vramはただの `[u8; 2048]` の純粋な配列）
impl Ppu {
    // 鏡写しのアドレス変換（魔法の窓口）
    pub fn mirror_vram_addr(&self, addr: u16) -> usize {
        // まず、0x2000〜0x2FFF の範囲を 0x0000〜0x0FFF (4096バイト分) に正規化する
        let mirrored_vram = addr & 0x2FFF;
        let vram_internal = mirrored_vram - 0x2000;
        
        // VRAMの1部屋の大きさは 1024バイト(1KB)
        let room_number = vram_internal / 0x0400; // どの部屋か？ (0, 1, 2, 3)

        match self.mirroring {
            Mirroring::Vertical => {
                // 垂直ミラー: 縦割り (部屋0,2 は 左側(実体0), 部屋1,3 は 右側(実体1))
                match room_number {
                    0 | 2 => (vram_internal % 0x0400) as usize,         // 実体0 (0〜1023)
                    1 | 3 => ((vram_internal % 0x0400) + 0x0400) as usize, // 実体1 (1024〜2047)
                    _ => unreachable!(),
                }
            }
            Mirroring::Horizontal => {
                // 水平ミラー: 横割り (部屋0,1 は 上側(実体0), 部屋2,3 は 下側(実体1))
                match room_number {
                    0 | 1 => (vram_internal % 0x0400) as usize,         // 実体0
                    2 | 3 => ((vram_internal % 0x0400) + 0x0400) as usize, // 実体1
                    _ => unreachable!(),
                }
            }
        }
    }
}
```

たったこれだけの `match` 条件分岐で、PPUが「部屋2（`$2800`）」や「部屋3（`$2C00`）」の大規模空間を平然と使って描画しようとしたとしても、裏側では勝手に実体のある `[u8; 2048]` の配列の中の正しい場所へ折り畳まれて書き込まれるようになります。

### 「マッパー」による途中の配線切り替え

その8で解説した「マッパー（MMC1等）」は、ここでも恐ろしい能力を発揮します。
ゲームの途中で「ボス戦に入ったから、急にスクロール方向を横から縦に変えたい！」と思ったとき、カセット内のマッパーチップに書き込み指令を送ることで、**物理的なはんだ付けと同じ「ミラーリングの配線設定」を、ゲーム中にプログラムから動的に（カチャッという音を立てて）切り替えることすら可能**なのです。

私達のRustのコードでは、単に `self.mirroring = Mirroring::Horizontal` と変数を上書きするだけで、その神業をシミュレーションすることができます。

スクロール（A章）とミラーリング（B章）。この2つの「配線とアドレスの魔法」を突破した時、あなたのエミュレータはただの静止画プレイヤーではなく、広大なファミコンの世界を旅する「冒険への窓」となるのです！
