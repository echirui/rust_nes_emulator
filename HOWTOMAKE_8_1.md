# NESエミュレータ開発のはじめかた（8-1：容量の壁とマッパーの誕生）

初期の『スーパーマリオブラザーズ』や『ドンキーコング』のような小さなファミコンソフトを動かすためのエミュレータは、ここまでの知識（CPU・PPU・コントローラ・NMI）さえあれば、実はすでに完成させることができます。

しかし、その後に発売された『ドラゴンクエストIII』や『星のカービィ 夢の泉の物語』といった大作ゲームのROMデータを読み込もうとすると、急に画面がバグったり、そもそも最初の赤い画面から進まなくなったりします。

この原因となるのが、後期の大容量カセットの中に物理的に搭載された特殊なICチップ、**マッパー（Mapper）** という存在です。
今回は、なぜマッパーが必要になったのか、そして「バンク切り替え」という魔法のような解決策について解説します。

---

## 1. ファミコンの最大の弱点「32KBの壁」

その3（アーキテクチャ）で、CPUにとっての世界は「全てが `0x0000` から `0xFFFF` までの 65,536個（64KB）の箱の並びである」と説明しました。
この限られた空間（バス）の中で、カセットの中のプログラム（PRG-ROM）を置くためにCPUが用意してくれている場所は、**`0x8000` 〜 `0xFFFF` までの「たったの32KB」** の範囲しかありません。

発売当初、任天堂の技術者は「32KBもあれば、これからのゲームは十分作れるだろう」と考えていました。実際、初代ドンキーコングなどはこの容量にピッタリ収まっています。

### 大容量化へのジレンマ
しかし、時代の流れとともにゲームの規模はどんどん巨大化（大容量化）していきました。
例えば『ドラゴンクエストIII』などは 256KB もの膨大なプログラム容量を持っています。32KBの8倍です。

ここで、エミュレータ開発においても最大のジレンマが発生します。
**「カセット自体の容量が256KBもあるのに、CPUには32KB分の本棚（窓）しか見えない！」**

CPUからすれば「`0x8000`から読めと言われても、本棚に入りきらないよ！残りの224KBのデータはどこに捨てればいいんだ？」となってしまい、ゲームが破綻してしまうのです。

---

## 2. 「ページをめくる」という物理的なカラクリ

この大問題を解決するため、任天堂や各ゲームメーカーが編み出した驚くべき解決策が**「バンク切り替え（Bank Switching）」**という物理的なハックです。

カセットのプラスチックの箱の中に、巨大なROMチップ（256KB）と一緒に、小さな**特殊なICチップ（マッパー）**を住まわせます。
CPUから見えている32KBの空間（`0x8000`〜`0xFFFF`）を、ただの「覗き窓」だと想像してください。
マッパーという名前の小人は、その窓の奥で巨大な256KBのデータ（プログラムの分厚い本）を抱えながら、CPUから要求された箇所だけをガチャン！と切り替えて（スライドさせて）窓枠にピッタリはめて見せてくれるのです。

```text
[ カセットの内部： 大容量ROM (256KB) ]
[ バンク0 ][ バンク1 ][ バンク2 ][ バンク3 ]... (各16KBなどの塊に分割)

             ↑(マッパーがガチャンと手動で切り替える)

[ CPUの世界： CPUから見える窓 (32KB: 0x8000〜0xFFFF) ]
今は窓枠に「バンク1と2 (合計32KB分)」が見えているよ！
```

### マッパーの種類は100種類以上！

この小人（マッパー）は、ゲームメーカーが「うちのゲームはもっと複雑な切り替えをしたい！」と独自のチップをカセットの中に勝手に組み込んだため、恐ろしいほどの種類が存在します。
（世界中で有志によって番号が振られており、例えば「マッパー0」は初期の切り替え無しカセット、「マッパー1」は任天堂製のMMC1（ゼルダの伝説など）、「マッパー4」はMMC3（スーパーマリオ3など）、コナミ製のVRC6など、数え切れないほどあります）

エミュレータが「ドラクエ3を動かす」ということは、CPUをシミュレーションするだけでなく、この**「カセットの中にいる独自の小人（マッパー）の思考回路」を、ゲーム（マッパー番号）ごとにエミュレータ側できちんとシミュレーションしてあげる**必要があるということを意味します。

---

## 3. 実機でのマッパーの操り方（あえて書き込む）

では、CPU側からプログラムを通して「おいマッパー、今は街のシーンが終わって戦闘に入るから、次のページ（戦闘用プログラムであるバンク3）を窓に映してくれ！」と指示を出すにはどうすればいいのでしょうか？

実はここでも、ファミコンならではの面白い（ハック的な）Memory Mapped I/O の仕組みが使われています。

CPUは、本来なら「カセットの中のプログラムを読み取る（Read）」ための場所である `0x8000`以上の空間に対して、**あえて「書き込み（Write）」** を行います。
ROM（Read Only Memory: 読み取り専用メモリ）なので、普通ならいくら数字を書き込んでも無視されます（エラーにもなりません）。

しかし、**カセットの中にいるマッパーは、CPUとROMの間でこの通信線をずっと盗み見張りしているのです**。

```rust
// CPUのプログラムのコード例:
// 「Aレジスタに "3" (切り替えタイ先のバンク番号) を入れて、0x8000 (ROMの番地) に書き込む！」
cpu.run_instruction(0xA9, 3); // LDA #3
cpu.run_instruction(0x8D, 0x00, 0x80); // STA $8000
```
これを受け取ったマッパーは、「なるほど！CPUは『バンク3』に切り替えてほしいんだな！」と察知し、瞬時に窓から見えるデータをスライドさせます。以降、CPUが同じ `$8000` の番地を読んだとしても、そこには全く新しいページ（バンク3の戦闘用プログラム）のデータが現れることになります。

次章（8-2）では、この「窓をスライドさせる小人」の実体を、Rustのコードでどうやって美しく表現していくのか、一番有名なマッパー（MMC1）の仕組みと具体的な実装イメージを見ていきます。
