# NESエミュレータ開発のはじめかた（2-2：高度なアドレッシングと命令マトリックス）

前回（2-1）で、同じ `LDA` 命令でも「手元（即値）」「身近な引き出し（ゼロページ）」「遠くの住所（絶対番地）」といった複数の**アドレッシングモード（探し方）**が存在することを学びました。

今回は、さらにプログラムらしく「Xレジスタを使ったループ」や「配列の処理」を実現するための複雑なアドレッシングモードと、ファミコンの全命令（オペコード）の正体について解説します。

---

## 1. アドレッシングモード（応用編）

### ④ ゼロページ X (Zero Page X / インデックス修飾)
手元のゼロページ領域（`$0000`〜`$00FF`）を探すときに、さらに **Xレジスタの値を足した場所** を探すモードです。
例えば、「クリボー3体の状態を順番に見る」といった、配列のループ処理（`array[x]` のような処理）をするときに大活躍します。

```text
命令: 0xB5 (LDA)
対象: 10 (0x0A)
条件: 事前に Xレジスタに「5」が入っているとする。
結果: 10 + 5 ＝ メモリの「15番地」に入っている数字を見に行き、Aレジスタに入れる。
```
※もし足した結果が255(0xFF)を超えた場合は、桁あふれ（ラップアラウンド）して0番地周辺に戻るというファミコン特有の動作（仕様でありバグ）があります。これを巧みに利用したゲームも存在します。

### ⑤ インダイレクト (Indirect / ポインタ)
さらに複雑な探し方として、C言語のポインタのような「住所が書かれた住所」を探しに行くモードもあります。
代表的なものに **インダイレクト Y（Indirect Y）** がありますが、直訳すると「間接参照にYレジスタのズレを含める」というもので、ゲームの「画面のスクロールデータ」など大量のデータを舐めるように読み込む際によく使われます。少々複雑なので、最初のうちは「そんなモードもあるんだな」程度に覚えておけば大丈夫です。

---

## 2. アドレッシングモードを Rust で表現しよう

「LDAだけでも探し方が複数（オペコード番号が複数）あるなら、他の命令も合わせると途方もない数の関数を作らなければいけないの？」と思うかもしれません。
もし、全てのオペコードごとに「メモリをどう読むか」の処理をバラバラに書いていると、コードが数千行のめちゃくちゃなスパゲッティ状態になってしまいます。

そこで、まずはこの「探し方」のリストをきれいに整理しましょう。

`src/main.rs` を次のように書き換えてみてください。

```rust
// データの見つけ方（アドレッシングモード）のリスト
#[derive(Debug)]
pub enum AddressingMode {
    Immediate,    // そのままの数字
    ZeroPage,     // ゼロページの番地
    ZeroPage_X,   // ゼロページの番地 + Xレジスタ
    ZeroPage_Y,   // ゼロページの番地 + Yレジスタ
    Absolute,     // 0000~FFFFのどこかの絶対番地
    Absolute_X,   // 絶対番地 + Xレジスタ
    Absolute_Y,   // 絶対番地 + Yレジスタ
    Indirect_X,   // Xレジスタを使った複雑なポインタ
    Indirect_Y,   // Yレジスタを使った複雑なポインタ
    NoneAddressing, // 対象の必要なし（レジスタ単体など）
}
```

そして、CPUに「このモードが来たら、こういう計算をして、最終的に探すべき『正解のアドレス（番地）』を返す」という専用の機能を持たせます。

```rust
impl Cpu {
    // 省略：レジスタなどの定義...

    // 指定されたモードに従って、対象となるメモリアドレスを計算して返す魔法の関数
    fn get_operand_address(&mut self, mode: &AddressingMode) -> u16 {
        match mode {
            AddressingMode::Immediate => {
                // "今読んでいる場所の次" そのものがデータ
                self.pc
            }
            AddressingMode::ZeroPage => {
                // "今読んでいる場所" にある数字を読み、それを0ページの番地として扱う
                self.mem_read(self.pc) as u16 
            }
            AddressingMode::ZeroPage_X => {
                // 読んだ数字に X レジスタを足す
                // （もし255を超えたら0に戻すために wrapping_add を使います）
                let pos = self.mem_read(self.pc);
                pos.wrapping_add(self.x) as u16
            }
            // ... (他のアブソリュートなどのモードも同様に、ルールに沿ってアドレスを計算します)
            _ => panic!("このモードはまだ作っていません！ {:?}", mode),
        }
    }
}
```

### LDA 関数の劇的なスリム化

この強力な `get_operand_address` 関数のおかげで、LDA 命令の実装は **たった1つ** の関数で済むようになります！

```rust
impl Cpu {
    // どんなモードであってもLDAができる関数 (`run_instruction`から呼ばれます)
    fn lda(&mut self, mode: &AddressingMode) {
        // 1. モードに従って、データの「場所（アドレス）」を見つける
        let addr = self.get_operand_address(mode);
        
        // 2. その場所からデータを読み取る
        let value = self.mem_read(addr);
        
        // 3. Aレジスタに入れる
        self.a = value;

        // 4. ゼロフラグなどのPレジスタを更新する（共通処理）
        self.update_zero_and_negative_flags(self.a); 
    }
}
```

これで、`0xA9(即値)` が来ても、`0xA5(ゼロページ)` が来ても、`0xB5(ゼロページX)`が来ても、全て一括で自動テストが可能になり、エミュレータ開発が一気に加速します。

---

## 3. 全体が見える！ 6502 命令マトリックス

「なんだか命令がいっぱいありそうで怖い」と思うかもしれませんが、恐怖の正体は先ほどの**アドレッシングモードとの組み合わせ（掛け算）** に過ぎません。

実際には、ファミコンの CPU (6502) には **たったの 56 種類** の目的（命令）しかありません。
まずは「何をしなければいけないか」の全体マップを見て、モチベーションを上げましょう！

### 🎮 データ操作と転送（前回・今回やったこと）
データの移動が主役です。ここで先ほどの `AddressingMode` がフル活躍します。レジスタに数字を入れたり、逆にレジスタの数字をメモリに保存（STA）したりします。

| 命令 | 役割 | 対応する主なモード |
| :--- | :--- | :--- |
| **LDA** | Aレジスタへ読み込み | Immediate, ZeroPage, ZeroPage_X, Absolute, Absolute_X... |
| **LDX**, **LDY** | X, Yレジスタへ読み込み | LDAとほぼ同じだが、_Xの代わりに_Yなどが使われる |
| **STA**, **STX**, **STY** | レジスタの中身をメモリへ書き込む | ZeroPage, Absolute など（Immediateは存在しない） |
| **TAX**, **TAY**, **TXA**, **TYA** | レジスタからレジスタへ直接コピー | `NoneAddressing` (アドレス計算が不要) |

### ➕➖ 算術と論理演算（計算チップの醍醐味）
足したり引いたり、ビットを入れ替えたりします。ここでも「メモリのどこから数字を持ってきて足すか」を決めるためにモードが活躍します。

| 命令 | 役割 |
| :--- | :--- |
| **ADC**, **SBC** | 繰り上がりつきの足し算、引き算（キャリー付き） |
| **AND**, **ORA**, **EOR** | ビット論理演算 (特定の部分だけONにしたりするマスク処理) |
| **INC**, **DEC** | メモリやレジスタの数字を +1, -1 する |
| **INX**, **INY**, **DEX**, **DEY**| X, Yレジスタを +1, -1 する（ループのカウンタに使われる） |
| **ASL**, **LSR**, **ROL**, **ROR** | ビットを左右にシフト(ずらす)して2倍や半分にする |

### ⤴️ 分岐とジャンプ（プログラムの醍醐味）
「もしAならこっちへ」という if文 のような分岐処理です。直前の「Pレジスタ（メモ帳）」の「ゼロフラグ」や「マイナスフラグ」が立っているかを判断して、「しおり（プログラムカウンタ PC）」をワープさせます。

| 命令 | 役割 |
| :--- | :--- |
| **JMP** | 無条件で指定したアドレスへ強制ジャンプ |
| **BEQ**, **BNE**, **BCC**, **BCS**... | フラグの条件を満たせば「現在位置から少しだけ前後にジャンプ」する |
| **JSR**, **RTS** | サブルーチン(関数)へのジャンプと、終わった後の元の場所への帰還 |

### 📦 スタックとフラグ操作（メモ帳の整理）
一時的にデータを退避したり、フラグ（メモ帳）を手動で書き換えたりします。

| 命令 | 役割 |
| :--- | :--- |
| **PHA**, **PHP**, **PLA**, **PLP** | スタック(メモリの端っこ)にAやPレジスタを一時保存・復帰 |
| **SEC**, **CLC**, **SEI**... | Pレジスタ(フラグ)を手動で ON / OFF する |
| **NOP** | なにもしない（ただ時間を潰す・タイミングを合わせる） |

---

## 4. 最後に

どうでしょうか？
オペコードの数は256個（未定義を含む）もありますが、その正体は **「56個の目的」×「数種類の見つけ方（アドレッシングモード）」** の掛け合わせで出来上がっているだけなのです。複雑なように見えて、非常に理路整然と整理されています。

次回（その3）からは、今回まとめたこの「目的」の関数（`adc()`, `beq()` など）の中から代表的なものをピックアップし、実際にレジスタがどう計算されていくのか、Rustでの実装とテストの書き方の中身に踏み込んでいきます！
