# NESエミュレータ開発のはじめかた（4-2：運命の分かれ道 / BEQ命令とプログラムの正体）

前回（4-1）で、ファミコンの計算の主役である「足し算（ADC）」と、そのときに発生する「繰り上がり（キャリーフラグ）」という、Pレジスタ（メモ帳）の重要な役割について学びました。

今回は、この**「計算結果として残されたフラグ（メモ帳）」を使って、プログラムの流れを変える（分岐する）命令**の仕組みについて解説します。
これこそが、単なる電卓と「ゲーム」を分けるプログラミングの醍醐味です。

---

## 3. 分岐する命令： BEQ (Branch if Equal)

「もし〜〜だったら、あっちへジャンプしろ！」という命令群を、**ブランチ（Branch / 枝分かれ）命令** と呼びます。
今回はその中で一番有名な `BEQ` を見てみましょう。

`BEQ` は直訳すると「等しければ分岐する（Branch if Equal）」ですが、実際には **「ゼロフラグ(Z)が 1 だったら、指定された歩数だけジャンプ（PC: プログラムカウンタを移動）する」** という動きをします。

### 「等しい」と「ゼロ」の関係

「等しいかどうか」調べたいのに、なぜ「ゼロ（0）かどうか」を見るのでしょうか？
実は、プログラミングの世界（特にアセンブリ言語）では、「2つの数字が同じかどうか？」を調べるために、**「AからBを引き算してみて、答えが 0 になるか？」**という計算を裏で行うからです。

たとえば、次のような流れでゲームは動いています。

1. 直前に、敵のHPからダメージ分を「引き算（SBCなど）」する計算を行う。
2. もし計算の答え（敵の残りHP）が `0` になった瞬間、Pレジスタの **ゼロフラグ(Z) が自動的にピコーン！と 1 になる**。
3. すぐあとに、この `BEQ` 命令を置いておくと、「もしゼロフラグが立っていたら（＝HPがゼロなら）、死んだとき用の専用アニメーション処理がある場所までジャンプする」という分岐が実現できるのです。

### Rustでの実装イメージ

```rust
impl Cpu {
    // 0xF0 (BEQ) のオペコードが呼ばれたときの処理
    fn beq(&mut self) {
        // 1. まず、自分のすぐ後ろに書いてある「ジャンプする距離(offset)」を読み込む。
        //    ※これは -128 から +127 までの符号付きの数字（前後への移動距離）です。
        //    (Rustでは i8 という 8ビットのプラスマイナス両方を持てる型として扱います)
        let offset = self.mem_read(self.pc) as i8;
        
        // （命令と距離を読み終わったので、PCを一つ次に進めておく）
        self.pc += 1; 

        // 2. 現在のPレジスタ（メモ帳）を見て、ゼロフラグ(Z)が立っているか判定する！
        //    (0b0000_0010 の位置にあるビットが 1 かどうか？)
        let z_flag = (self.p & 0b0000_0010) != 0;
        
        // 3. もしゼロフラグ(Z)が立っていたらジャンプ実行！
        if z_flag {
            // 現在のPC(しおり)の場所に、読み取った歩数(offset)を足す。
            // これで次に読む場所がガバッと飛ぶ！
            self.pc = self.pc.wrapping_add(offset as u16);
            
            // （※実機の6502CPUでは、見事ジャンプに成功した時は
            //     「遠くへ飛ぶための準備時間」として、余分に1サイクル時間がかかります）
        }
        // Zフラグが0だった場合（HPが残っているなど）は、このif文は無視され、
        // 何事もなかったかのようにすぐ真下の普通のプログラムが実行され続けます。
    }
}
```

### レジスタの変化図（Zフラグが立っており、+5歩だけジャンプする場合）

レジスタ群のなかで、「次にどの行を読むか」を管理している「プログラムカウンタ(PC / しおり)」の数字だけがガバッと書き換わります。

```text
[ 実行前 ]                            [ 実行後 ( BEQ +5 実行) ]
📝 P  = [ Z:1 (ゼロだったよ！) ]      📝 P  = [ Z:1 ]  (フラグはそのまま)

🎯 PC = 0x8000   ======( +5歩 )=====> 🎯 PC = 0x8005 (次に読む場所が飛んだ！✈️)
```

逆に、もし実行前のPレジスタで `Z:0` だった場合はジャンプ失敗となり、`PC = 0x8001` （計算が済んで1つ進んだ場所）となり、そのまま真下に続く命令が実行されます。

ファミコンには、これと全く逆の「ゼロ **じゃなかったら** ジャンプしろ！(`BNE`)」や、「キャリー（繰り上がり）が **発生していたら** ジャンプしろ！（`BCS`）」など、同じ仕組みの「分岐兄弟」たちがいっぱいいます。（しかもRustでの実装は、このif文の条件を `!z_flag` や `c_flag` に変えるだけです！）

---

## 4. プログラムとは「3つのブロック」の組み合わせに過ぎない

この（その1〜4）までの非常に長い旅を通して、エミュレータ開発の一番の土台作りが終わりました。
一見、難解な暗号の羅列に見えるファミコンのマシン語プログラムも、蓋を開ければたった3つの動きの繰り返しで出来上がっていることがわかります。

1. **データを持ってくる・移動する** (`LDA`, `STA`, `TAX`...)
2. **計算して、自働的にフラグ(メモ)を残す** (`ADC`, `SBC`, `INC`...)
3. **さっきのフラグ(メモ)を見て、別ルートへジャンプする** (`BEQ`, `JMP`...)

この3つのブロックをレゴブロックのように組み立てることで、スーパーマリオのジャンプ軌道も、ドラクエのダメージ計算も、すべてが表現されているのです。

次回からは、いよいよこの「頭脳（CPU）」を抜け出し、この計算結果をどのようにしてテレビ画面に映していくのかという、**PPU（グラフィックチップ）とROM（カセット）の世界** へ足を踏み入れていきましょう。
