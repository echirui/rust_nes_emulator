# NESエミュレータ開発の壁（C-1：カセットデータの正体「iNESファイル」）

これまでのガイドで「CPUの仕組み」や「PPUの描画」、そして「マッパーの特殊能力」を学んできましたが、開発の本当に一番最初にぶつかる壁があります。

「そもそも、インターネットからダウンロードした（あるいは自分で吸い出した） `.nes` という拡張子のファイルは、プログラムの中で**どうやって中身を読み解けば（パースすれば）いいのか？**」

今回は、世界中のエミュレータ開発者が基準としている事実上の標準フォーマット、**「iNES（アイ・ネス）フォーマット」** の読み解き方について解説します。

---

## 1. `.nes` ファイルの中身を覗いてみる

`.nes` ファイルは、「スーパーマリオのプログラムやグラフィックがそのまま純粋に並んでいるだけのファイル」ではありません。
なぜなら、前回の（その8章）で学んだ通り、ファミコンのカセットは「初期容量の小さいもの」から「ドラクエのように独自の特殊マッパーチップを搭載したもの」まで多種多様だからです。

エミュレータにただ生のデータだけを渡しても、「このカセットには何KBのプログラムが入っているのか？」「どのマッパー小人を呼び出せばいいのか？」が分かりません。

そこで、`.nes` ファイルの先頭には必ず **16バイトの「説明書（ヘッダー）」** がくっつけられています。

```text
[ .nes ファイルの全体構造 ]

+-------------------------+
| 0〜15バイト: iNESヘッダー  | ← 「このゲームの設定」が書かれた16文字
+-------------------------+
| (オプションのTrainer等)   |
+-------------------------+
| PRG-ROM データ          | ← CPUが読むプログラム (16KBの倍数)
| (例: 32KB なら 32768文字) |
+-------------------------+
| CHR-ROM データ          | ← PPUが読むグラフィック (8KBの倍数)
| (例: 8KB なら 8192文字)  |
+-------------------------+
```

---

## 2. 最初の16バイト（iNESヘッダー）の解読

Rustで `.nes` ファイル（ただの `Vec<u8>` バイト配列です）を読み込んだら、まずは最初の16個の数字を取り出し、それぞれが何を意味しているのかを解読します。

### バイト 0〜3: マジックナンバーの合言葉
最初の4バイトは、**必ず `N` `E` `S` `\x1A` (16進数で `0x4E, 0x45, 0x53, 0x1A`)** という文字配列になっています。
これは「このファイルは絶対にファミコンのROMファイルだよ！」という証明（マジックナンバー）です。
もし違っていたら、即座にエラーを出してプログラムを止める必要があります。

### バイト 4: プログラム（PRG）はどれくらい大きい？
この1バイトには、**「カセットに入っているPRG-ROM（プログラム領域）が、16KBブロックの単位で何個分あるか」** が書かれています。
例えば、ここに `2` と書かれていれば「16KB × 2 ＝ 32KB（32,768バイト）」のプログラムデータがこの後に入っていることが分かります。

### バイト 5: グラフィック（CHR）はどれくらい大きい？
この1バイトには、**「CHR-ROM（画像スタンプ領域）が、8KBブロックの単位で何個分あるか」** が書かれています。
例えば、ここに `1` と書かれていれば「8KB（8,192バイト）」のグラフィックデータがカセットに入っています。
（※マッパーによっては、CHR-ROMを持たず本体内のRAMを使うゲームがあり、その場合はここが `0` になります）

### バイト 6: コントロール1（特殊設定とマッパー番号の下半分）
ここには、ビットごとに複数の設定が詰め込まれています。
*   **0ビット目 (右端)**: `1` なら「垂直ミラーリング」、`0` なら「水平ミラーリング」（スクロールの壁：章B で解説した配線設定です）
*   **1ビット目**: `1` なら「バッテリーバックアップ用のSRAMがある（ドラクエのようにセーブできる）」
*   **2ビット目**: `1` なら「Trainerデータ（チート等）がPRGの前に512バイトある」
*   **3ビット目**: 4画面ミラーリングか？
*   **4〜7ビット目**: **「マッパー番号（下4桁）」**

### バイト 7: コントロール2（マッパー番号の上半分）
*   **4〜7ビット目**: **「マッパー番号（上4桁）」**
*   その他: （iNES1.0フォーマットでは予約済み、またはiNES2.0の複雑な設定など）

### バイト 8〜15: 予約領域（ゼロ埋め）
昔の基本的なiNESフォーマットでは、残りの8バイトは使われません（すべて `0x00`）。将来の拡張のために取ってありました。

---

## 3. なぜマッパー番号が「半分ずつ」分かれているの？

バイト6とバイト7のそれぞれ「上の4桁」に、マッパー番号が半分ずつ（ニブル単位で）格納されているのはなぜでしょうか？
これは、最初期にフォーマットを作った人（iNESエミュレータ作者のMarat Fayzullin氏）が、「マッパーなんて15個（4桁分）もあれば全ゲーム網羅できるだろう」と思い込んで バイト6 だけに設定を作ったのが原因です。

しかし後日、「しまった！カセットの特殊チップの種類が100個を超えてしまった！」と慌てて、余っていた隣の バイト7 の上半分に拡張枠として下駄を履かせたという、歴史的な継ぎ接ぎ（魔改造）の名残なのです。

次章（C-2）では、この愛すべき継ぎ接ぎのヘッダーを、Rustのコードでどうやって切り刻んで（パースして）、「PRG」と「CHR」の純粋なデータリストを取り出し、ROM構造体を作成するのかを解説します。
