# NESエミュレータ開発のはじめかた（3-1：全体マップと魔法の道路「バス」）

これまで学んできた「CPU」と「命令（オペコード）」の知識があれば、理屈の上ではすべての計算ができるようになります。
しかし、エミュレータとは単なる電卓ではありません。「画面に絵を出し」「コントローラーの入力を受け付け」「音を鳴らす」という、複数のパーツ（基板上のチップ）が連携して初めてゲームが動きます。

今回は、CPUという主人公がどのようにして世界（他のチップ）と繋がっているのか、ファミコンの**アーキテクチャ（全体構造）**について解説します。

---

## 1. ファミコンの見取り図（アーキテクチャ）

CPUはエミュレータの心臓ですが、単体では画面を出すことも音を鳴らすこともできません。ファミコン全体がどう繋がっているか、まずは全体の地図を見てみましょう。

```text
               +-------------------------------------------------+
               |                   Bus (バス)                      |
               |                                                 |
               |    (0x0000 - 0xFFFF のメモリ空間を管理する道路)    |
               +-------------------------------------------------+
                 ^                    ^                   ^
                 | 読み/書き            | 読み/書き           | 読み/書き
                 v                    v                   v
      +--------------------+ +--------------------+ +--------------------+
      |        CPU         | |        PPU         | |        APU         |
      | (Ricoh 2A03/6502)  | |  (Picture Proc.)   | |   (Audio Proc.)    |
      |                    | |                    | |                    |
      | ゲームのロジック実行 | | 画面の描画・VRAM等 | | 矩形波やノイズ等の |
      | 計算・コントローラ | | ※CPUの3倍の速さ  | | サウンド生成       |
      +--------------------+ +--------------------+ +--------------------+
                 ^                    ^
                 | 読み/書き            | 読み専用 (グラフィックデータ)
                 v                    v
      +---------------------------------------------------+
      |               カセット (Cartridge)                |
      |                                                   |
      |  PRG-ROM (プログラム)         CHR-ROM (グラフィック)  |
      +---------------------------------------------------+
```

### 中央を貫く巨大な道路「バス（Bus）」

この地図の最大のポイントは、**Bus（バス）** という名前の巨大な管理システムが中央に鎮座していることです。
パソコンを自作したことがある人なら「マザーボード」を想像すると分かりやすいかもしれません。全てのパーツはこの「バス」という1つの大通りに接続されています。

CPUは、このバス上にある `0x0000`から`0xFFFF`までの **「65,536個（64KB）の番地（住所）」** に向かって、「〇〇番地にあるデータを読め（Read）」「〇〇番地にデータを書け（Write）」という指示を出すことしかできません。

---

## 2. Memory Mapped I/O の魔法

ここで一つの疑問が浮かびます。
「CPUは番地に対して読み書きしかできないなら、どうやって画面を描画する（PPUに指示を出す）の？」

実は、ファミコン（だけでなく多くの昔のコンピュータ）では、**「特定の番地（住所）」に読み書きをすると、それが自動的に特定のチップへの通信にすり替わる** という「Memory Mapped I/O（メモリ・マップド・アイオー）」という魔法が使われています。

CPUから見ると「ただの配列（引き出し）」に書き込んでいるつもりでも、バス（Bus）側で「あ！今CPUさんが2000番台に書き込んだぞ！それはPPUさん宛ての手紙だ！」と自動で配送先を振り分けてくれるのです。

### ファミコンの「住所録（メモリマップ）」

これがファミコンにおける絶対のルール、**メモリマップ（Memory Map）** です。

*   **`0x0000` ～ `0x07FF` (本体RAM)**:
    ただの純粋な「メモ帳」です。マリオの現在位置やスコア、残機など、ゲームの中で変化する普通の変数がここに保存されます。
*   **`0x2000` ～ `0x2007` (PPUレジスタ窓口)**:
    ここが**PPU（グラフィックチップ）への直通ダイヤル**です。CPUがここに数字を書き込むと、それがそっくりそのまま「画面のスクロール位置を変えろ」「パレットの色を変えろ」というPPUへの指示にすり替わります。
*   **`0x4000` ～ `0x4015` (APUレジスタ窓口)**:
    ここが**APU（サウンドチップ）への直通ダイヤル**です。ここに数字を書き込むと、「高い音を出せ」「ノイズを鳴らせ」という音の指示になります。
*   **`0x4016` (コントローラー入力窓口)**:
    ただの番地に見えて、ここを「読む（Read）」と、その瞬間にプレイヤーが押している「十字キーやAボタンの状態」が届くという面白い仕掛けになっています。
*   **`0x8000` ～ `0xFFFF` (PRG-ROM / カセットのプログラム)**:
    この広い空間は、ファミコン本体に刺さっている「カセット」内のプログラム（マリオなどのゲーム自体のコードや音楽データ）が直接読めるように繋がっています。

### Rust での Bus（バス）の実装イメージ

先ほどの「住所による自動振り分け（Memory Mapped I/O）」を、Rustのコード（`match`文）で表現してみましょう。非常にシンプルに実装できることが分かります。

```rust
pub struct Bus {
    ram: [u8; 2048], // 0x0000~0x07FF の本体RAM
    ppu: Ppu,        // PPU（グラフィックチップ）への参照
    rom: Vec<u8>,    // カセットのプログラムデータ
    // ...その他 APUなど
}

impl Bus {
    // CPUが「この番地を読みたい！」と言ってきたときの処理
    pub fn mem_read(&mut self, addr: u16) -> u8 {
        match addr {
            // 0x0000 から 0x1FFF までは、本体RAM（メモ帳）の管轄
            0x0000..=0x1FFF => {
                let mirror_down_addr = addr & 0x07FF; // (ファミコン特有の繰り返し仕様)
                self.ram[mirror_down_addr as usize]
            }
            // 0x2000 から 0x3FFF までは、PPU（グラフィック）の管轄！
            0x2000..=0x3FFF => {
                let mirror_down_addr = addr & 0x2007; // PPUの窓口は8個だけ
                // PPUの専用関数を呼び出して、結果を返す
                self.ppu.read_register(mirror_down_addr)
            }
            // 0x8000 以上はカセット（ROM）の管轄！
            0x8000..=0xFFFF => {
                // ROMの中からプログラムのデータを取り出して返す
                let rom_addr = addr - 0x8000;
                self.rom[rom_addr as usize]
            }
            // どこにも当てはまらない場合はとりあえず0を返す
            _ => 0,
        }
    }

    // 書き込み（mem_write）も同じように、番地を見て適切なチップにデータを送ります。
}
```

このように、CPUの実装では「ひたすらに番地を読んで、計算して、番地に書く」ことだけを忠実に守り、中継地点である **Bus（バス）クラス** で条件分岐させるだけで、すべての部品が魔法のように連携し始めるのです。

次章（3-2）では、この空間の繋がりにもう一つの次元、**「時間（クロックサイクル）の同期」** というエミュレータ最大の命題について解説します。
