# NESエミュレータ開発のはじめかた（9-2：Rustで作るシンセサイザーと音のミキシング）

前回（9-1）で、ファミコンの「デジタルな音の正体（波のON/OFF）」と、APUが「8歩で1サイクルのペースで波を作り出す」というハードウェアの設計思想を学びました。

今回は、この「配列を使った足踏み」をRustのコードでどうやって実現し、最終的に5つの楽器の音をパソコンのスピーカーに届けるのか、そのエミュレータ実装の心臓部を見ていきます。

---

## 3. RustでのAPU（Pulse波）の実装イメージ

APUの各チャンネル（楽器）は、「タイマー（音の高さ）」「長さカウンタ（鳴らす時間）」「エンベロープ（音の減衰）」などの部品が精密に組み合わさってできています。
ここでは一番基本となる「矩形波（Pulseチャンネル）」が、どうやってあのピコピコ音を作り出しているのか、核となる部分を抜き出して見てみましょう。

```rust
pub struct PulseChannel {
    // ---- CPUからの命令によって書き込まれる設定値 ----
    pub timer: u16,        // 音の高さを決めるタイマー(周波数の基準値)
    pub duty_mode: u8,     // 波の形（ONとOFFの比率。例えば12.5%や50%など）
    pub volume: u8,        // 出力する最大の音量 (0〜15)

    // ---- APU自身がリアルタイムで動かすための内部変数 ----
    pub timer_value: u16,  // 今カウントダウンしている途中の残り時間
    pub duty_step: u8,     // 現在の「8歩」の波のサイクルのどこにいるか(0〜7)
}

impl PulseChannel {
    // エミュレータの時計(CPUのクロック)が進むごとに呼ばれる関数
    pub fn tick(&mut self) {
        // 1. タイマー（残り時間）があるなら、それを減らす
        if self.timer_value > 0 {
            self.timer_value -= 1;
        } else {
            // 2. タイマーが0になった瞬間（時間が来た！）
            // 時間を元の設定値の長さに戻して（リロードして）、
            self.timer_value = self.timer;
            
            // 3. 波の階段(duty_step)を「1歩」だけ前へ進める。
            // 8歩目までいったら最初(0)に戻す(% 8を使います)
            self.duty_step = (self.duty_step + 1) % 8;
        }
    }

    // 今この一瞬、このチャンネルから「どれくらいの数字（波の高さ）」が出ているかを計算する
    pub fn output(&self) -> u8 {
        // ファミコンの矩形波は「8歩で1サイクルの波」を律儀に繰り返す。
        // duty_mode(デューティ比)によって、8歩のうちのどの配列を使うかが決まる。
        let is_on = match self.duty_mode {
            0 => [0,1,0,0,0,0,0,0][self.duty_step as usize], // 12.5% (細くピーキーな音)
            1 => [0,1,1,0,0,0,0,0][self.duty_step as usize], // 25.0% (普通のピコピコ)
            2 => [0,1,1,1,1,0,0,0][self.duty_step as usize], // 50.0% (太くブーッという音)
            3 => [1,0,0,1,1,1,1,1][self.duty_step as usize], // 25.0%の反転
            _ => 0,
        };

        // 配列から拾ってきた今の場所が `1` (ON) なら、設定された音量をそのまま出力！
        if is_on == 1 {
            self.volume 
        } else {
            // `0` (OFF) なら、無音(0)を出力する
            0 
        }
    }
}
```

このように、APUもPPUと同じ様に「CPUのクロック（時間）の針をチクタクと少しずつ消費しながら、自分の中のタイマーをごりごり減らし、ONとOFFを機械的に切り替えていく」という、電子回路の超高速なシミュレーションに過ぎません。
CPUのプログラムは、ただ時々この `PulseChannel.timer` などを外から違う数字に書き換えて（調律して）いるだけなのです。

---

## 4. 全ての音を混ぜる（オーディオ・ミキシング）

ここまでは、Pulseチャンネル1つだけを見ました。
しかし、エミュレータ本体の仕事は、先ほど作ったような5つの独立したチャンネル全員から出力される `output()` の値（それぞれ 0〜15 の数字）を集めて、**ミキサー（計算式）を使って1つのブレンドされた波（`f32`の小数など）にまとめる** ことです。

```rust
// APU全体を管理し、5つの音を1つの波にまとめるイメージ
impl Apu {
    // 毎秒48000回のペースで、完成した1つめの「音のサンプル」を取り出す
    pub fn get_sample(&self) -> f32 {
        // 各楽器に「いまの君の音量を教えて！」と聞いて回る
        let p1 = self.pulse1.output() as f32; // メロディ1
        let p2 = self.pulse2.output() as f32; // メロディ2
        let tri = self.triangle.output() as f32; // ベース
        let noi = self.noise.output() as f32; // ドラム
        let dmc = self.dpcm.output() as f32; // 声など

        // （※注意：実機のアナログ回路では、単純な足し算ではなく、
        // 音が重なるほど少しずつ音が潰れていく(非線形)という特性があります。
        // 以下はそれをシミュレーションして近似したミキシングの方程式です）
        let pulse_out = 95.88 / ((8128.0 / (p1 + p2)) + 100.0);
        let tnd_out = 159.79 / ((1.0 / (tri / 8227.0 + noi / 12241.0 + dmc / 22638.0)) + 100.0);
        // (安全のため分母が0になったときの回避処理などは省略しています)
        
        // 最終的な「全チャンネルをブレンドした、この一瞬の波の高さ(0.0〜1.0)」を返す
        pulse_out + tnd_out
    }
}
```

その6「時間の同期」で解説したように、エミュレータのメインループは、パソコンのスピーカーのタイミングに合わせて定期的にこの `get_sample()` を呼び出します。
そして、得られた `f32` の数字を配列の「バッファ箱」に何百個も詰め込み、箱がいっぱいになったら `audio_tx.send(samples)` でOSのスピーカー設定（winitやcpalなど）へ放り投げています。

### オーディオ開発に潜む「最大のシビアさ」

なぜエミュレータ全体の動作時間を「CPUの命令タイミング」ではなく、「オーディオの再生」にロックさせているのでしょうか。
描画のグラフィック（マリオのアニメーションなど）は、人間の目には1フレーム（1/60秒）くらい描かれないコマ落ちがあっても「ちょっとカクついたかな？」としか感じません。

しかし、**オーディオ（耳）は「たった1ミリ秒分でも途切れたり、タイミングのズレが生じたりすると、ブチッ、バリッという猛烈な強烈なノイズ（音割れ）として聞こえてしまう」** というシビアな難しさがあります。人間の耳の分解能は恐ろしいほど高いのです。
だからこそ、エミュレータ開発においては「音のバッファ箱（送信）」を最優先の時計として扱い、それが溢れたらエミュレータ全体を一時停止させるという、強固な同期テクニックが必要になるのです。

---

## 5. ファミコンエミュレータ開発の終着点へ

これで、ファミコンを構成する全ての「目・耳・手足・頭脳」のパーツについて触れました。

*   **CPU**: `$0000`〜`$FFFF`の番地から司令書を読み込み、ひたすら計算をする「頭脳」
*   **PPU**: `$2000`〜`$2007`を受け取り、ビームのタイミングを合わせて「目（映像）」を高速で作る職人
*   **Joypad**: `$4016`を通じて、現実のプレイヤーの手元の「手足（入力）」を1ビットずつ伝える窓口
*   **Mapper**: あえて書き込みを受け取るハックで、32KBの窓から巨大なROMをパタンと「めくる」小人
*   **APU**: `$4000`〜`$4015`でタイマーを回し、「耳（音楽）」を作り出し、すべての時間を支配するシンセサイザー

これらすべてが「バス（Bus）」という1つの道路で繋がり、CPUとPPUの歩む「時間（クロック数）」が完全に同期したとき、あの感動のゲーム体験があなたの書いたプログラム（PCの画面）の上で息を吹き込みます。
長かった座学はこれにて完了です。

ここから先は、ぜひ実際のCPUのオペコードのRust実装や、PPUの描画のコードを書きながら、**自分だけのエミュレータ（仮想の世界）を完成させていきましょう！**
