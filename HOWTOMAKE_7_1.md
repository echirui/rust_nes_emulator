# NESエミュレータ開発のはじめかた（7-1：プレイヤーの魂、コントローラとシフトレジスタ）

これまでに「CPUの計算・PPUの描画・NMIによる60FPSの時間同期」という、ゲームを動かすための3つの巨大な柱が完成しました。
しかし、このままでは電源を入れても、画面の中でタイトル画面のロゴが光り、キャラクターが勝手に動くだけの「デモ画面（アトラクトモード）」しか見られません。

最後にして最大の喜びである**「自分でマリオを操作する」**ための仕組み、つまり**「コントローラ（Joypad）」**のハードウェア的な制約と、それをプログラムでどう復元しているのかについて解説します。

---

## 1. コントローラのボタンは1バイト（8ビット）の塊

ファミコンの1Pコントローラ（1コン）には、おなじみの「A、B、Select、Start、上、下、左、右」の合計8つのボタンがついています。（※ツーコンのマイク機能などはここでは割愛します）

プログラムの世界では、ボタンというものは本質的にアナログな押し込みではなく、「押されているか（1）」「押されていないか（0）」というデジタルの情報を扱います。
**この8個のボタンのON/OFF状態は、ちょうど「1バイト（8ビット）」の数字という箱に綺麗に収まって表現することができます**。

```text
[ コントローラのボタンとビットの対応表（一般的な並び順） ]

ビット番:   7    6    5    4    3    2    1    0
ボタン名:   A    B   Sel  Str  Up   Dn   Lt   Rt
--------------------------------------------------
(例)        1    0    0    0    0    0    0    1
```

※ たとえばマリオで「Aボタンでジャンプしながら、右ボタンで進んでいる」状態のとき、コントローラの内部状態は `10000001` (16進数で `0x81`、10進数で `129`) という一つの数字として表現されます。

---

## 2. コスト削減の天才的なアイデア：「シフトレジスタ」

さて、ゲーム空間にいるCPUのプログラム（マリオ）は、「今、この瞬間にAボタンは押されているか？」を知りたいのですが、ここで一つハードウェア的（物理的）な面白い制約にぶつかります。

それは、**CPUが入っている本体基板と、コントローラを繋ぐケーブルの「線の数が圧倒的に足りない」** ということです。

8個のボタン全部の状態を「今すぐ全部（1バイトすべて）教えて！」と同時に送るには、通信用の線が最低8本横並びで必要になります（パラレル通信）。
しかし、当時のファミコンではコスト削減のため、データを送受信する線を**たった1本**しか用意していませんでした（シリアル通信）。

その代わり、**「シフトレジスタ」**という電気的な仕掛けを使って、「1ビットずつ順番にボタンの状態を尋ねる」という天才的な方法でこの問題を解決しています。

### CPUからコントローラ窓口（`$4016`）への通信ルール

CPUは、おなじみのMemory Mapped I/O機能を使って、自分の中に繋がっているメモリ番地 **`$4016`** を通じて、ケーブルの先にあるコントローラと会話します。

#### ステップ1. 写真を撮る（ラッチ）

まず、CPUが `$4016` に対して `1` を書き込み、直後に `0` を書き込みます（これを ストロボ機能、またはラッチ と呼びます）。
すると、コントローラ側の小さなICチップは「あ、今ボタンの状態を聞かれる準備の合図だ！」と察知し、**その瞬間の8つのボタンの押し込み状態を、自分の手元のシフトレジスタという箱に「カシャッ」と写真のように保存・固定**します。
（これ以降、プレイヤーが指を離しても、送るべきデータは固定されたままになります）

#### ステップ2. 1個ずつ順番に読み出す（シフトアウト）

その後、CPUが `$4016` から**データを1回読み取るごとに**、先ほど固定したボタンの状態が「A、B、Select、Start、上、下、左、右」という決まった順番で、**1本の線を伝って1つ（1ビット）ずつポロッと出てきます**。

```text
CPU「$4016に 1 を書いて、さらに 0 を書くよ！」
コントローラ「ラッチ完了！今のボタン状態(A, B...)を固定しました！」

CPU「じゃあ $4016 を読むよ（1回目）」 ->  コントローラ「はい、Aボタンは押されているので 1 」
CPU「次 $4016 を読むよ（2回目）」 -> コントローラ「はい、Bボタンは押されていないので 0 」
CPU「次 $4016 を読むよ（3回目）」 -> コントローラ「はい、Selectボタンは... 0 」
...（これを律儀に8回繰り返す）
```

### ソフトウェアの努力

つまり、ファミコンのゲームプログラム（スーパーマリオのROMの中のコード）は、毎フレームの最初（NMIが起きた直後）に、わざわざこの `$4016` を何度も叩き、面倒な「8回読みとって1ビットずつ足し合わせるループ」を自力で実行しています。
ハードウェアの線を減らした分を、ソフトウェア（CPUの計算回数）の努力でカバーしているのです。

---

この「1ビットずつ順番に返す」という面白いギミック（シフトレジスタの挙動）を、エミュレータを作る側である私達はどうやってコードで表現すればいいのでしょうか？
次章（7-2）では、この仕組みを Rust で実装し、最終的にパソコンのキーボードと繋ぎ合わせるまでの具体的なコードを見ていきます。
