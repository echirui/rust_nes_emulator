# NESエミュレータ開発のはじめかた（3-2：エミュレータ最大の命脈「時間の同期」）

前回（3-1）で、巨大な配列である「Bus（バス）」を経由して、CPUがPPU（グラフィック）やAPU（サウンド）に直接指示を出せる仕組み（Memory Mapped I/O）を作りました。
これで「空間的な繋がり」は完成です。

しかし、エミュレータにおいて「正しく空間が繋がっていること」と同じくらい大切なのが、**「正しい時間を刻むこと」**です。

---

## 3. 実機と同じ「時間」を刻む（クロックサイクル）

これまで「Aレジスタに数字を入れる（LDA）」といった命令を作ってきましたが、現実のICチップにおける電気回路の動きには必ず**「物理的な時間」**がかかります。
これをエミュレータの世界では **クロックサイクル数（Cycles）** と呼びます。

実機と同じ速度で動作するエミュレータを作るためには、計算結果だけでなく「その作業にどれくらい時間がかかったか（何サイクル消費したか）」も正確にシミュレーションする必要があります。

### 時計の針をサボるとどうなるか？

現代のパソコンのCPUは、ファミコンの数万倍の速さで計算ができます。
もし「時間（サイクル）」をシミュレーションせずに全力でファミコンのコードを動かしてしまうと、ゲーム内でマリオが「1秒間に画面を100往復する」ほどの超スピードで動いてしまい、全くゲームになりません。
また、音楽の再生スピードも爆速になり、そもそもPPU（グラフィックチップ）が絵を描き終える前に次の座標が計算されてしまうため、画面がバグだらけになってしまいます。

### 命令とモードによる「手間（時間）」の違い

「何サイクルかかるか」は、前回学んだ **アドレッシングモード（探し方）** によって細かく決められています。

例えば同じ `LDA` 命令であっても、以下のようになります。

| オペコード | 命令 | モード | 作業内容 | 必要なサイクル数 |
| :---: | :---: | :--- | :--- | :---: |
| **`0xA9`** | **LDA** | Immediate | 「すぐ手前の数字をポンッとAに入れる」 | ⏱️ **2 サイクル** |
| **`0xA5`** | **LDA** | ZeroPage | 「身近な引き出し(`0x00~0xFF`)を開けてAに入れる」 | ⏱️ **3 サイクル** |
| **`0xAD`** | **LDA** | Absolute | 「遠くの住所(`0x0000~0xFFFF`)まで歩いてAに入れる」 | ⏱️ **4 サイクル** |
| **`0xB1`** | **LDA** | Indirect_Y| 「XやYの計算をしてから住所を割り出し、遠くへ探しに行く」| ⏱️ **5〜6 サイクル** |

このように、「より遠くへ探しに行く」「より計算が複雑になる」ほど、CPUが消費する時間（サイクル数）が律儀に増えていきます。

※さらにマニアックな話をすると、条件分岐（`BEQ`：もしゼロならジャンプ）などは、「ジャンプしなかったら2サイクル」「ジャンプが発生したら3サイクル」「さらにページを跨ぐ遠いジャンプなら4サイクル」というように、**実行時の状況に応じて動的にかかる時間が変わる** のも、エミュレータ開発を面白くしている要因の一つです。

---

## 4. Rust で「時間」を表現する関数

これをエミュレータのコードでどう表現するのでしょうか？以前作ったCPUの `step` メソッド（1回だけ命令を実行する関数）を、次のように改良します。

```rust
impl Cpu {
    // 戻り値として「この命令にかかった物理的な時間(サイクル数)」を返すようにする
    pub fn step(&mut self) -> u8 {
        // 1. 今のPCの番地から、命令（オペコード）を読む
        let opcode = self.mem_read(self.pc);
        self.pc += 1;

        // 2. 命令によって処理を変えつつ、かかった「サイクル数」を決定する
        let cycles = match opcode {
            // LDA: 即値 (Immediate) は 2サイクル
            0xA9 => {
                self.lda(&AddressingMode::Immediate);
                2
            }
            // LDA: ゼロページ (ZeroPage) は手間がかかるので 3サイクル
            0xA5 => {
                self.lda(&AddressingMode::ZeroPage);
                3
            }
            // TAX: 手元での移動作業なので パパッと 2サイクル
            0xAA => {
                self.tax();
                2
            }
            // ... 他の命令も同じように実装していく
            _ => panic!("知らない命令です: {:02X}", opcode),
        };

        // 3. 呼び出し元（メインループ）に「これだけ時間がかかったよ」と教えてあげる
        cycles
    }
}
```

---

## 5. 究極の魔法「サイクルの同期（Cycle Sync）」

この新しく生まれ変わった「時間を計算できる `step()` メソッド」のおかげで、エミュレータの大元（アーキテクチャ図のさらに外側のループ）で、**「各チップの時間の流れを完全に一致させる」** という魔法が使えるようになります。

```rust
// エミュレータのメインループのイメージ (Nes.update() など)
loop {
    // 1. CPUを1命令だけ実行し、その命令に「何サイクルかかったか」を聞く
    //    （例： ここで 3 サイクルかかったとする）
    let cpu_cycles = cpu.step();

    // 2. PPU（画面描画チップ）はCPUの【3倍の速さ】で動く設計になっている。
    // そのため、いまCPUが消費した時間(サイクルの3倍)だけ、画面の描画処理を進める！
    for _ in 0..(cpu_cycles * 3) {
        ppu.tick(); // ビームを1ドットずつ進めてTV画面を描く処理
    }

    // 3. APU（サウンドチップ）はファミコンではCPUと【ほぼ同じ速さ】で動く。
    // CPUが消費した時間と同じだけ時間を進める
    for _ in 0..cpu_cycles {
        apu.tick(); // 音の波の形を進める処理
    }
    
    // （※このあと、現実世界の時間「1秒間に60回」に合わせる処理が入ります）
}
```

これがエミュレータにおける **「サイクルの同期（Cycle Sync）」** という最重要の考え方です。

これによって、「CPUが複雑な配列の計算（5サイクルの重い命令）をしている間は、グラフィックであるPPUもきっちり15回（5サイクル×3倍）だけ働き、画面の描画を先に進めておく」という、リアルの物理的なファミコン基板の上で起きている並行作業を、プログラム上で見事にシミュレーションすることができるのです！

次章（その4）からは、様々な動作の要となる具体的なファミコンプログラム（ADC, BEQ等）の実装と、その際のレジスタの動きを図解で見ていきましょう。
