# NESエミュレータ開発のはじめかた（1-1：エミュレータの基礎とCPUの骨格づくり）

## はじめに：エミュレータ開発の壁を越えよう

エミュレータ開発と聞くと、「グラフィック描画の高度な数学的知識が必要なのでは？」「音声を扱うための難しいプログラミング技術が要るのでは？」など、ものすごくハードルが高いように思えるかもしれません。
しかし、こと「ファミコン（NES）」のエミュレータに関して言えば、その心臓部である「CPU（中央演算処理装置）」の仕組みは、驚くほどシンプルにできています。

エミュレータの基本は、**「メモリ（配列）から数字を読み取って、決められたルールに従って足したり引いたりして、またメモリに書き戻す」** という、単調な作業の繰り返しに過ぎません。魔法のように動くマリオやドラクエも、この「超高速な足し算と引き算」の集積によって成り立っています。

このチュートリアルでは、プログラミング初心者の方でも理解できるように、専門用語を日常生活の「モノ」に例えながら、「まずは1つだけ命令（オペコード）を実行できる小さなCPU」を作り、それが正しく動くことを確認するところから始めます。

---

## 1. なぜRustを使うのか？プロジェクトの準備

今回、エミュレータ開発の言語として **Rust** を採用します。エミュレータは「メモリの正確な読み書き」と「高速な処理（1秒間に数百万回）」が求められる分野です。
Rustを使えば、C言語やC++のように高速でありながら、「メモリの読み間違え」や「意図しないデータの破壊」といったバグを、コンパイルの時点で言語側が防いでくれます。（後々、これがものすごく助けになります！）

### プロジェクトの作成

まずは Rust のプロジェクトを作成しましょう。ターミナル（コマンドプロンプト）を開いて、以下のコマンドを実行します。

```bash
cargo new my_nes
cd my_nes
```

このコマンドによっで `my_nes` という名前の新しいフォルダが作られ、その中にひな形となるコードファイルが自動生成されます。
自動生成された `src/main.rs` をお気に入りのエディタで開き、少し書き換えてみましょう。

```rust
// src/main.rs
fn main() {
    println!("NES Emulator の開発スタート！");
}
```

まずはこれを実行して、開発環境が正しく動いているか確認します。

```bash
cargo run
```

画面に `NES Emulator の開発スタート！` と表示されれば、エミュレータ開発の第一歩を踏み出したことになります。

---

## 2. 最初の「CPU」を作ってみよう：レジスタの世界

ファミコンに搭載されているCPU（通称：リコー 2A03 / ベースはMOS 6502という有名なチップ）は、現代のパソコンのCPUと比べると非常に小規模です。

CPUの強さは「レジスタ」と呼ばれる **CPU専用の小さな引き出し（超高速な記憶領域）** の数で決まると言っても過言ではありませんが、ファミコンのCPUにはプログラマが使えるメインの引き出しが **たったの4つ** しかありません。

まずはプログラムの中で、このCPUの構造（枠組み）を定義してみましょう。

`src/main.rs` に以下のコードを追加します。

```rust
// CPUの構造を定義する構造体(struct)
pub struct Cpu {
    pub a: u8,      // アキュムレータ（Aレジスタ）
    pub x: u8,      // Xレジスタ（インデックス用）
    pub y: u8,      // Yレジスタ（インデックス用）※今回は使いませんが後で登場します
    pub p: u8,      // ステータスレジスタ（計算結果の感想を書き残すメモ帳）
    pub pc: u16,    // プログラムカウンタ（今どこを読んでいるかの『しおり』）
}
```

### なぜ `u8` と `u16` が混ざっているの？

ここで型の違いについて説明しておきましょう。

*   **`u8` (Unsigned 8-bit integer)**: 0 から 255 までのプラスの数字が入る、とても小さな箱です。ファミコンは「8ビット機」と呼ばれる通り、**データを扱う箱は基本的にすべてこの `u8` サイズ（0〜255）** になります。A、X、Y などのデータ用引き出しはすべてこれです。
*   **`u16` (Unsigned 16-bit integer)**: 0 から 65,535 までの数字が入る、少し大きな箱です。プログラムカウンタ（`pc`）は「メモリの何番地か」を指し示します。ファミコンのメモリは全体で約6万5千個あるため、住所を指し示す際には 0〜255(u8)の数字では到底足りず、必ず `u16` が使われます。

### CPUの誕生（インスタンス化）

型を定義しただけでは設計図に過ぎません。実際にこの設計図から「まっさらなCPU」を生み出すための初期化関数を作りましょう。

```rust
impl Cpu {
    // 新しいCPUを生み出す（初期化する）ための関連関数
    // 慣例として `new` という名前をよく使います。
    pub fn new() -> Self {
        Cpu {
            a: 0,
            x: 0,
            y: 0,
            p: 0, // ※実際には初期状態特有の特殊なルールがありますが、今回は簡単のため0とします
            pc: 0,
        }
    }
}
```

これで、プログラムの中に「まっさらな状態のCPUの実体」が誕生する準備が整いました。
しかし、このままではCPUは何もすることができません。命令を与え、それを解読し、行動を起こさせる機能が必要です。

次章（1-2）では、このCPUに最初の知識（命令）を教え込み、実際に数字を動かしている様子を観察してみましょう。
