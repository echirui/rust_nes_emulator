# NESエミュレータ開発のはじめかた（6-2：目覚まし時計（NMI）と60FPSの同期）

前回（6-1）で、テレビの画面は「走査線」というビームで上から下へ描かれており、マリオの座標を書き換える（VRAMを触る）ためには、ビームが止まっている一瞬の隙**「VBlank期間（241段目〜）」**を狙わなければならないことを学びました。

今回は、CPUがその「隙」をどうやって知るのか？ そしてエミュレータ全体がどうやって「毎秒60回」という一定の速度を保っているのかについて解説します。

---

## 4. PPUからCPUへの合図「NMI」

「VBlank期間を狙えと言われても、CPU側はどうやってそのタイミングを知るのか？」
ここで登場するのが、**NMI (Non-Maskable Interrupt：マスク不可割り込み)** というファミコン最強の仕組みです。

PPUは、241段目に突入して**VBlankが始まった瞬間に、文字通りハードウェア（ピン）の電気信号を使って、CPUへ「今だ！画面描き終わったぞ！」という強力なシグナル（NMI）を直接送信します**。

この信号を受け取ると、CPUは今の作業（足し算だろうがループだろうが）を強制的に中断させられ、特別な行動を取らざるを得なくなります。

### NMIを受け取ったCPUの強制的な動き

1. 「今やっていた計算途中のデータ（PCやPレジスタのメモ帳）」を、後で正確に戻ってこれるようにスタック（メモリの端っこの一時退避場所）にプッシュして保存する。
2. 割り込み中の再割り込みを防ぐため、自分自身の「割り込み無効フラグ」をONにする。
3. メモリの最後のほう（`$FFFA` 番地と `$FFFB` 番地）に書かれている「避難先の住所（アドレス）」を読む。
4. その住所に書かれている場所に**強制ワープ（PCを書き換え）**して、そこから新しいプログラムを実行し始める。

このワープ先のプログラム（**NMIハンドラ**と呼ばれます）には、ゲームのプログラマがあらかじめ「VBlankが来たら、OAM DMA($4014)を発動してマリオを動かし、背景の設計図($2007)を更新しろ！」という処理を書いておきます。

```rust
// エミュレータ側のRustコードでの NMI の実装イメージ
impl Cpu {
    // PPUからNMI信号（true）を受け取ったときに呼ばれる関数
    pub fn nmi(&mut self) {
        // 1. 今のPC(しおり)とステータス(Pレジスタ)をスタック(一時退避場所)にプッシュする
        self.stack_push_u16(self.pc);
        
        // （※NMIだよ、という印(Bit5)と共にPレジスタを保存する）
        self.stack_push_u8(self.p | 0b0010_0000); 

        // 2. 割り込み中のフラグ(I)を立てて、他の邪魔を防ぐ
        self.p = self.p | 0b0000_0100;

        // 3. 一番最後のメモリ（$FFFA / $FFFB）に書いてある住所(NMIベクタ)を読み取る
        let nmi_address = self.mem_read_u16(0xFFFA);

        // 4. 強制的にそこへワープして、次のクロックからNMIハンドラを実行開始する！
        self.pc = nmi_address;
    }
}
```

NMIの中の処理（マリオの更新など）がピタッと終わると、CPUは `RTI (Return from Interrupt)` という特別な命令（オペコード）を使って、先ほどスタックにしまっておいた「中断した作業の続き」へ何事もなかったかのようにこっそりと戻ります。

CPUにとってNMIは、**現実世界で例えるなら「毎秒60回、問答無用で鳴り響く目覚まし時計」**のようなもので、このアラームによって全てのゲームの時間の流れ（1フレーム）が進んでいくのです。

---

## 5. エミュレータはどうやって60FPSを維持しているのか？

その3（アーキテクチャ）で説明した同期コードをもう一度見てみましょう。
今度は、時間（クロックサイクル）と走査線（VBlank）、そして目覚まし時計（NMI）の意味がよりはっきりと分かるはずです。

```rust
// 動作し続ける無限ループ (エミュレータの心臓部)
loop {
    // ---- 1枚の絵（フレーム）を作るためのループ ----
    loop {
        // 1. CPUを1歩だけ進めて、その命令に「何サイクルかかったか」を聞く
        let cycles = cpu.step(); 

        // 2. PPUをその3倍進める（スキャンラインのビームを律儀に少しずつ動かす）
        for _ in 0..cycles * 3 {
            
            // PPUが1ドットずつ処理を進める...（tick関数）
            if ppu.tick() {
                // tick() が true を返した！
                // つまり「PPUのビームがちょうど241段目(VBlank)に到達した！」
                
                // CPUの背中を叩いて「NMI（割り込み）」を強制発動させる！
                cpu.nmi();
            }
        }

        // PPUが261段(+VBlankの空白期間)までをすべて終えて、
        // 「さあ次の一枚を描くぞ」と準備ができたらこのフレームは終了
        if ppu.frame_ready { break; } 
    }

    // ---- オーディオの「長さ」に合わせて現実時間を足止めする ----
    
    // エミュレータ内では1フレーム(1/60秒分)が完成した。
    // そのフレームの音声波形(配列データ)を、パソコン本体のスピーカーへ送る。
    let samples = cpu.bus.get_audio_buffer();
    
    // もしスピーカーが「まだ前の0.01秒前の音を再生中だよ！」と言ってきたら、
    // ここで sender.send() 関数が（スピーカーが空くまで）エミュレータ全体の動作を一時停止させる。
    // 
    // これにより、自然と「毎秒60回」のペースでしかエミュレータが進まなくなり、
    // マリオが早送りになるのを物理的に防ぐことができる！
    audio_tx.send(samples);
}
```

この芸術的な構造を見てください。

*   **PPUのビーム（走査線）** が時間を測り、VBlankに入ると**目覚まし時計（NMI）** を鳴らして CPU の行動を支配する。
*   一番外側にある**音の送信ブロック（Audio Sync）** が、完成した「エミュレータ世界」の時間の流れを、私達の「現実時間（パソコンの時計）」にがっちりとロックする。

全ての役者がこの絶妙なバランスで組み合わさることで、あの懐かしいファミコンのゲームが、私達の現代のパソコンの上で見事によみがえるのです！

このように、「チップ単体の理屈」と「それらが組み合わさって流れる時間」の違いを知ることこそが、エミュレータ開発の最大の壁であり、最大の楽しさでもあります。
次章（その7）では、ついにこの世界にプレイヤーの「魂の入力」を送り込む、コントローラの実装について解説します。
