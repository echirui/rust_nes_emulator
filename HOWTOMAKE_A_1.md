# NESエミュレータ開発の壁（A-1：マリオはどうやって横に進むのか？ スクロールの基礎）

これまでのガイド（1〜9）で、CPUの計算、PPUの描画タイミング、音出し、そしてコントローラ入力まで、エミュレータの基礎となる骨組みが完成しました。単一画面のゲーム（ドンキーコングやパックマンなど）であれば、これだけでも遊ぶことができます。

しかし、いざ『スーパーマリオブラザーズ』を起動して右に歩き始めると、エミュレータ開発者は最初の巨大な壁にぶつかります。マリオが画面端に行っても、**画面が全く横にスクロール（移動）しない** のです。

今回は、ファミコンにおける最大の難所の一つである「スクロール機能」と、PPUの内部構造について解説します。

---

## 1. ネームテーブルの限界と「覗き窓」の概念

その5（PPUのキャンバス）で、VRAMには「ネームテーブル（画面の設計図）」という 32マス×30マス（＝1画面分） の黒板があることを学びました。

しかし、スーパーマリオの1-1の広大なステージを考えてみてください。あの長いステージの全てが、最初からPPUの小さな黒板に描かれているわけではありません。
ファミコン本体が持っているVRAMの容量はたったの **2KB（ちょうど画面2枚分のみ）** しかないのです。

ではどうやってスクロールしているのでしょうか？
実は、PPUの描画機能というのは、**「2枚の黒板を横に並べてループさせ、その上をテレビ画面サイズ（256x240ピクセル）の『覗き窓（カメラ）』がスライドしていく」** という仕組み担っています。

```text
[ VRAM上の2枚の黒板（ネームテーブル） ]

+--------------------+--------------------+
| 黒板0 (最初の画面) | 黒板1 (次の画面)   |
| MARIO ->         |                  |
+--------------------+--------------------+
      ======(覗き窓カメラを右にスライド)=====>
```

マリオが右に進むと、カメラ（覗き窓）も右にズレていきます。
そして窓の左端から見えなくなった黒板0の古いデータは、CPUが裏でコソコソと（VBlankの隙間で）「そのさらに先の新しいブロックの絵」に書き換えて再利用しているのです。

この「永遠に続くベルトコンベアー」のような仕組みで、限りないスクロールを実現しています。

---

## 2. PPUスクロール窓口（`$2005`）の秘密

CPUから「カメラ（覗き窓）の位置を右に動かして！」と指示するために使われるのが、**`$2005` (PPUSCROLL)** というPPU専用の秘密の窓口です。

画面の座標（ドット単位）は「X（横）」と「Y（縦）」の2つあります。それぞれ 0〜255 までの値を指定します。
CPUは、この `$2005` 番地に連続して **2回書き込み** を行うことで、カメラの位置を設定します。

```rust
// CPUのプログラムの動き: 「カメラを横にX=100ドット、縦にY=50ドット 動かせ！」

// 1回目の書き込み： PPUはこれを「X座標（横スクロール）」として記憶する
cpu.mem_write(0x2005, 100); 

// 2回目の書き込み： PPUは自動的にこれを「Y座標（縦スクロール）」として記憶する
cpu.mem_write(0x2005, 50);  
```

非常にシンプルに見えます。しかし、エミュレータ開発を地獄に突き落とすのは、この「2回書き込む」という仕様の裏に隠された **PPU内部の異常なアドレス計算回路** なのです。

### なぜスクロールの実装は「地獄」なのか？

エミュレータを作る側は、この `$2005` への書き込みを受け取って、実際のPPUの描画処理（毎フレーム数万回のループ）の最中に「このドットを描くときは、VRAMの〇〇番地（ネームテーブルと属性テーブル）にズラして色を読みにいかなければ」という計算をしなければなりません。

しかし、当時の任天堂のハードウェア設計者は、「スクロール用のX/Y座標」と、「VRAMのどこの番地を読み書きしているか（`$2006`で設定したアドレス）」を、チップの回路を節約するためになんと **「1つの共通の15ビットレジスタ」の中でぐちゃぐちゃに混ぜ合わせて計算する** という魔改造じみた設計を採用してしまったのです。

つまり、`$2005`（スクロール指示）に書き込むことと、`$2006`（VRAMの住所指定）に書き込むことは、**全く同じPPU内部の「現在位置レジスタ」の別々のビット（桁）を書き換えているに過ぎない** のです。

次章（A-2）では、このエミュレータ開発界隈で伝説となっている難解なレジスタ、通称 **「Loopy Register（ルーピー・レジスタ）」** の全貌と、それをRustのコードでどう美しく解きほぐして実装するのかを解説します。
